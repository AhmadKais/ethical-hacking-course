<div dir="rtl">

# 📘 מודול 2 — יסודות Linux (Linux Fundamentals) — כל החומר בקובץ אחד

> קובץ זה מרכז את **כל** תוכן המודול לגלילה רצופה, לפי סדר הלימוד: **חומר → תרגילים → תרגול → פתרונות**.
> הקבצים הנפרדים (`README`, `missions`, `practice`, `solutions`, `slides`) עדיין קיימים לצד קובץ זה.

**ניווט מהיר:** [📖 חומר הלימוד](#material) · [🎯 תרגילים (Missions)](#missions) · [🧠 תרגול (Practice)](#practice) · [✅ פתרונות (Solutions)](#solutions)

---

<a id="material"></a>
# 📖 חומר הלימוד

> **מטרות המודול:** לשלוט בשורת הפקודה של Linux ברמה הנדרשת לבדיקות חדירה — ניווט במערכת הקבצים, ניהול קבצים ותיקיות, הרשאות ומשתמשים, `sudo`, ניהול חבילות ושירותים, פקודות רשת, תהליכים וחיפוש. זהו הבסיס המעשי לכל שאר הקורס.
>
> **קבצים:** `README.md` (חומר) · [`missions.md`](missions.md) (תרגילים) · [`solutions.md`](solutions.md) (פתרונות) · [`practice.md`](practice.md) (תרגול) · [`slides.md`](slides.md) (מצגת).
>
> **מבוסס על שיעורי המקור** #03–#09 (Sudo, Navigating the Filesystem, Users & Privileges, Common Network Commands, Viewing/Creating/Editing Files, Services, Installing Tools).

---

## תוכן העניינים
1. [השֶׁל (Shell) ושורת הפקודה](#21-השל-shell-ושורת-הפקודה)
2. [מבנה מערכת הקבצים](#22-מבנה-מערכת-הקבצים-filesystem-hierarchy)
3. [ניווט](#23-ניווט-navigation)
4. [עבודה עם קבצים ותיקיות](#24-עבודה-עם-קבצים-ותיקיות)
5. [צפייה ועריכת קבצים](#25-צפייה-ועריכת-קבצים)
6. [משתמשים, קבוצות והרשאות](#26-משתמשים-קבוצות-והרשאות)
7. [sudo והרשאות-על](#27-sudo-והרשאות-על)
8. [ניהול חבילות והתקנת כלים](#28-ניהול-חבילות-והתקנת-כלים)
9. [ניהול שירותים](#29-ניהול-שירותים-services)
10. [פקודות רשת נפוצות](#210-פקודות-רשת-נפוצות)
11. [תהליכים](#211-תהליכים-processes)
12. [חיפוש וצינורות](#212-חיפוש-צינורות-וטריקים)

---

## 2.1 השֶׁל (Shell) ושורת הפקודה

**השֶׁל (Shell)** הוא התוכנית שמפרשת את הפקודות שאתה מקליד. ב-Kali ברירת המחדל היא **Bash** (וכיום לעיתים ZSH). ה-**Terminal** הוא החלון שבו אתה מקליד.

### אנטומיה של ה-Prompt
```text
┌──(kali㉿kali)-[~]
└─$
```
- `kali` — שם המשתמש
- `kali` (אחרי הסימן) — שם המחשב (Hostname)
- `~` — התיקייה הנוכחית (`~` = תיקיית הבית)
- `$` — משתמש רגיל · `#` — משתמש-על (root)

> 💡 **טיפים שיחסכו לך זמן:**
> - **Tab** — השלמה אוטומטית של שמות קבצים/פקודות.
> - **↑ / ↓** — מעבר בין פקודות קודמות.
> - **`history`** — כל הפקודות שהרצת. `!42` מריץ מחדש את פקודה מס' 42.
> - **`clear`** (או `Ctrl+L`) — ניקוי המסך.
> - **`man <פקודה>`** — המדריך המלא של כל פקודה (`q` ליציאה).

---

## 2.2 מבנה מערכת הקבצים (Filesystem Hierarchy)

ב-Linux **הכל הוא קובץ**, והכל מתחיל מהשורש `/` (Root). אין "כונן C:" — יש עץ אחד.

| נתיב | תפקיד | רלוונטיות לתקיפה |
|------|-------|------------------|
| `/` | השורש — ראש העץ | — |
| `/etc` | קובצי הגדרות המערכת | `/etc/passwd`, `/etc/shadow` — יעדים חמים |
| `/home` | תיקיות המשתמשים | קבצים אישיים, מפתחות SSH |
| `/root` | תיקיית הבית של root | פרס גדול אם מגיעים לשם |
| `/var` | לוגים ונתונים משתנים | `/var/www` (אתרים), `/var/log` (לוגים) |
| `/tmp` | קבצים זמניים | מקום נוח להעלות כלים בזמן תקיפה |
| `/usr` | תוכנות וספריות | `/usr/share/wordlists` |
| `/bin`, `/sbin` | פקודות מערכת | הכלים עצמם |
| `/opt` | תוכנות שהותקנו ידנית | כלים שהורדנו מ-GitHub |

> 🔍 **שני קבצים שכל האקר מכיר:**
> - `/etc/passwd` — רשימת המשתמשים (קריא לכולם).
> - `/etc/shadow` — ה-Hash של הסיסמאות (קריא ל-root בלבד). גישה אליו = פרס.

---

## 2.3 ניווט (Navigation)

```bash
pwd                 # Print Working Directory — איפה אני עכשיו
ls                  # רשימת קבצים בתיקייה
ls -l               # פירוט מלא (הרשאות, בעלים, גודל, תאריך)
ls -la              # כולל קבצים מוסתרים (שמתחילים ב-.)
ls -lh              # גדלים בפורמט קריא (KB/MB)
cd /etc             # מעבר לתיקייה (נתיב מוחלט)
cd Downloads        # מעבר לתת-תיקייה (נתיב יחסי)
cd ..               # תיקייה אחת אחורה (הורה)
cd ~                # לתיקיית הבית
cd -                # חזרה לתיקייה הקודמת
tree                # תצוגת עץ של התיקיות
```

### נתיב מוחלט מול יחסי
- **מוחלט (Absolute):** מתחיל מ-`/`, למשל `/home/kali/Desktop`. עובד מכל מקום.
- **יחסי (Relative):** ביחס למיקום הנוכחי, למשל `Desktop` או `../etc`.
- קיצורים: `.` = התיקייה הנוכחית · `..` = ההורה · `~` = הבית.

> 💡 **דוגמה:** אם אתה ב-`/home/kali` והרצת `cat ../../etc/passwd` — `..` פעמיים מעלה אותך ל-`/`, ומשם ל-`etc/passwd`.

---

## 2.4 עבודה עם קבצים ותיקיות

```bash
touch file.txt          # יצירת קובץ ריק
mkdir mydir             # יצירת תיקייה
mkdir -p a/b/c          # יצירת שרשרת תיקיות
cp file.txt copy.txt    # העתקה
cp -r dir1 dir2         # העתקת תיקייה (רקורסיבי)
mv file.txt /tmp/       # העברה
mv old.txt new.txt      # שינוי שם
rm file.txt             # מחיקת קובץ
rm -r mydir             # מחיקת תיקייה
rm -rf mydir            # מחיקה בכוח (זהירות!)
```

> ⚠️ **אזהרה:** ל-Linux אין "סל מיחזור". `rm -rf` מוחק לצמיתות ומיידית. חשוב פעמיים לפני שמריצים אותו, ולעולם לא על `/`.

---

## 2.5 צפייה ועריכת קבצים

### צפייה
```bash
cat file.txt        # הדפסת כל הקובץ
less file.txt       # צפייה עמוד-עמוד (q ליציאה, / לחיפוש)
head file.txt       # 10 השורות הראשונות
head -n 20 file.txt # 20 שורות ראשונות
tail file.txt       # 10 השורות האחרונות
tail -f /var/log/syslog  # מעקב חי אחרי לוג (שימושי מאוד)
wc -l file.txt      # ספירת שורות
```

### הפניית פלט (Redirection)
```bash
echo "hello" > file.txt     # כתיבה (דורס את הקובץ)
echo "world" >> file.txt    # הוספה בסוף (לא דורס)
command > out.txt 2>&1       # הפניית פלט ושגיאות לקובץ
```

### עריכה — nano (הכי פשוט)
```bash
nano file.txt
# Ctrl+O = שמירה, Ctrl+X = יציאה
```

### עריכה — vim (חזק, נפוץ בשרתים)
```bash
vim file.txt
# i = מצב עריכה (Insert)
# Esc = חזרה למצב פקודות
# :wq = שמירה ויציאה   |   :q! = יציאה ללא שמירה
```

> 💡 **טעות נפוצה:** "נתקעתי ב-vim ואי אפשר לצאת". הפתרון: `Esc` ואז הקלד `:q!` ו-Enter.

---

## 2.6 משתמשים, קבוצות והרשאות

```bash
whoami          # מי אני
id              # ה-UID, ה-GID והקבוצות שלי
who             # מי מחובר כרגע
cat /etc/passwd # כל המשתמשים במערכת
```

### קריאת שורת הרשאות
כשמריצים `ls -l` רואים משהו כזה:
```text
-rwxr-xr--  1 kali kali  1024 Jan 1 script.sh
```
פירוק ה-`-rwxr-xr--`:
| חלק | משמעות |
|-----|--------|
| `-` | סוג (`-` קובץ, `d` תיקייה, `l` קישור) |
| `rwx` | הרשאות ה**בעלים** (Owner) — קריאה/כתיבה/הרצה |
| `r-x` | הרשאות ה**קבוצה** (Group) |
| `r--` | הרשאות **כולם** (Others) |

- **r** = read (4) · **w** = write (2) · **x** = execute (1)

### שינוי הרשאות ובעלות
```bash
chmod +x script.sh       # הוספת הרשאת הרצה
chmod 755 script.sh      # rwx לבעלים, r-x לשאר (7=rwx,5=r-x)
chmod 644 file.txt       # rw לבעלים, r לשאר
chown kali:kali file.txt # שינוי בעלים וקבוצה
```

> 🔍 **קשר לתקיפה — קבצי SUID:** קובץ עם ביט SUID רץ בהרשאות **הבעלים** שלו, לא של המריץ. אם קובץ בבעלות root מוגדר SUID — ניתן לנצלו להסלמת הרשאות (מודול 11). איתור:
> ```bash
> find / -perm -4000 2>/dev/null
> ```

---

## 2.7 sudo והרשאות-על

**root** הוא משתמש-העל (UID 0) — יכול הכל. **`sudo`** ("Super User DO") מריץ פקודה יחידה בהרשאות root.

```bash
sudo apt update         # הרצת פקודה כ-root
sudo -l                 # אילו פקודות מותר לי כ-sudo? (חשוב מאוד!)
sudo su                 # מעבר למעטפת root מלאה
whoami                  # (אחרי sudo su → root)
```

- קובץ ההגדרות: `/etc/sudoers` (עורכים עם `visudo`).

> 🔍 **קשר לתקיפה:** `sudo -l` הוא מהדברים הראשונים שבודקים אחרי השגת גישה — הרשאות sudo שגויות הן דרך קלאסית להסלמת הרשאות ל-root (מודול 11).

---

## 2.8 ניהול חבילות והתקנת כלים

Kali מבוססת Debian, ומשתמשת ב-**APT**:
```bash
sudo apt update                 # רענון רשימת החבילות
sudo apt upgrade -y             # עדכון החבילות
sudo apt install nmap -y        # התקנת כלי
sudo apt remove nmap -y         # הסרה
apt search <keyword>            # חיפוש חבילה
dpkg -i package.deb             # התקנת חבילת .deb ידנית
```

### התקנה מ-GitHub (כלים שאינם ב-APT)
```bash
git clone https://github.com/aboul3la/Sublist3r.git
cd Sublist3r
pip install -r requirements.txt   # התקנת תלויות Python
python3 sublist3r.py -d target.com
```

> 💡 סדר הפעולות הרגיל אחרי התקנת Kali נקייה: `sudo apt update && sudo apt full-upgrade -y`.

---

## 2.9 ניהול שירותים (Services)

**שירות (Service / Daemon)** הוא תוכנית שרצה ברקע (שרת web, SSH, בסיס נתונים).

```bash
sudo systemctl start ssh       # הפעלת שירות
sudo systemctl stop ssh        # עצירה
sudo systemctl restart ssh     # הפעלה מחדש
sudo systemctl status ssh      # בדיקת מצב
sudo systemctl enable ssh      # הפעלה אוטומטית בעליית המערכת
# תחביר ישן שעדיין עובד:
sudo service apache2 start
```

> 🔍 **דוגמה מעשית:** כדי להעביר קובץ למכונת מטרה, מפעילים שרת web מקומי:
> ```bash
> sudo systemctl start apache2       # מגיש את /var/www/html
> # או שרת פייתון מהיר:
> python3 -m http.server 8000
> ```

---

## 2.10 פקודות רשת נפוצות

```bash
ip a                 # כתובות IP ו-MAC (מחליף את ifconfig)
ip route             # טבלת הניתוב (Gateway)
ping -c 4 10.0.0.1   # בדיקת קישוריות (4 חבילות)
ss -tuln             # פורטים פתוחים מקומית (מחליף netstat)
netstat -tulnp       # פורטים + התהליכים שמאזינים
cat /etc/resolv.conf # שרת ה-DNS
curl http://10.0.0.5 # שליפת דף web
wget http://.../file # הורדת קובץ
ssh user@10.0.0.5    # התחברות מרוחקת מאובטחת
nslookup google.com  # תרגום שם ל-IP
```

> 💡 את הרשת עצמה (OSI, TCP/UDP, פורטים) נלמד לעומק ב[מודול 3](../03-networking/). כאן המיקוד הוא בפקודות.

---

## 2.11 תהליכים (Processes)

```bash
ps aux               # כל התהליכים הרצים
ps aux | grep ssh    # סינון לתהליך מסוים
top                  # תצוגה חיה של תהליכים (q ליציאה)
kill 1234            # סיום תהליך לפי PID
kill -9 1234         # סיום בכוח
./script.sh &        # הרצה ברקע
jobs                 # משימות הרקע
```

---

## 2.12 חיפוש, צינורות וטריקים

### חיפוש
```bash
find / -name "*.conf" 2>/dev/null   # חיפוש קבצים לפי שם
find / -perm -4000 2>/dev/null      # חיפוש קבצי SUID
grep "password" file.txt            # חיפוש טקסט בקובץ
grep -r "api_key" /var/www          # חיפוש רקורסיבי בתיקייה
which nmap                          # היכן נמצא כלי
locate rockyou.txt                  # חיפוש מהיר (דורש updatedb)
```

### צינורות (Pipes) ושרשור
ה-**Pipe** (`|`) מעביר את הפלט של פקודה אחת כקלט לשנייה — כלי עוצמתי:
```bash
cat /etc/passwd | grep root         # רק שורות עם root
ps aux | grep apache | wc -l        # כמה תהליכי apache רצים
cat access.log | grep "404" | sort | uniq -c   # ספירת שגיאות 404
```
- `&&` — הרץ את הבא רק אם הקודם הצליח: `apt update && apt upgrade`
- `;` — הרץ ברצף ללא קשר להצלחה.
- `>` `>>` — הפניית פלט (סעיף 2.5).

> 💡 **דוגמה משולבת מהעולם האמיתי:**
> ```bash
> find / -perm -4000 2>/dev/null | grep -v snap
> ```
> מוצא קבצי SUID לצורך הסלמת הרשאות, ומסנן החוצה רעש — בדיוק מה שעושים אחרי פריצה.

---

## סיכום המודול

- **השֶׁל** מפרש פקודות; Tab, ↑, `history` ו-`man` חוסכים זמן.
- **מערכת הקבצים** היא עץ אחד מ-`/`; `/etc/passwd` ו-`/etc/shadow` הם יעדים חמים.
- **ניווט** (`cd`, `ls`, `pwd`) ו**ניהול קבצים** (`cp`, `mv`, `rm`, `mkdir`).
- **צפייה ועריכה** (`cat`, `less`, `nano`, `vim`) והפניית פלט (`>`, `>>`).
- **הרשאות** (rwx, `chmod`, `chown`) ו-**sudo** (`sudo -l`) — קריטיים להסלמת הרשאות.
- **APT** להתקנה, **systemctl** לשירותים, **ip/ping/ss/curl** לרשת.
- **תהליכים** (`ps`, `top`, `kill`) ו-**חיפוש+צינורות** (`find`, `grep`, `|`).

### מה הלאה?
➡️ [**תרגילים — `missions.md`**](missions.md) · [**פתרונות — `solutions.md`**](solutions.md) · [**תרגול ותרחישים — `practice.md`**](practice.md)
➡️ [**מודול 3 — יסודות רשתות**](../03-networking/)

[⬅️ חזרה למפת הקורס](../../README.md)

---

<a id="missions"></a>
# 🎯 תרגילים (Missions)

> כל התרגילים ב-Terminal של Kali. פתרונות מלאים ב-[**`solutions.md`**](solutions.md) — נסה לבד קודם!

> ### 🚦 לפני שמתחילים — קרא אותי!
> - **מה צריך:** מכונת **Kali** דולקת עם **Terminal** פתוח. זהו. אין צורך ביעד חיצוני.
> - **איך עובדים:** קרא קודם את ה-[`README.md`](README.md). כאן נהיה **מאוד ישירים** — לרוב התרגילים נותנים את **הפקודה המדויקת** ואת **מה לחפש בפלט**. פשוט הקלד ובדוק.
> - **נתקעת?** אם פקודה מחזירה שגיאה — קרא אותה לאט (לרוב שם/נתיב שגוי). נסה שוב, ואז הצץ ב-[`solutions.md`](solutions.md).
> - **טיפ:** אלה הפקודות שתשתמש בהן בכל שאר הקורס. שווה לשנן אותן ע"י חזרה.

מקרא רמות: 🟢 קל · 🟡 בינוני · 🟠 מתקדם · 🔴 אתגר

---

## חלק א' — ניווט ומערכת הקבצים (🟢 קל)

**2.1** 🟢 — הצג את התיקייה הנוכחית, עבור ל-`/etc`, והצג את תוכנה.
```bash
pwd
cd /etc
ls
```
  👀 **חפש:** אחרי `cd /etc` הפקודה `pwd` תראה `/etc`, ו-`ls` תציג הרבה קבצי הגדרות (passwd, hosts...).
  ✅ הצלחה: אתה ב-`/etc` ורואה את הקבצים שבו.

**2.2** 🟢 — חזור לתיקיית הבית בשתי דרכים שונות.
```bash
cd ~        # דרך 1: הקיצור לבית
cd          # דרך 2: cd ללא ארגומנט = הביתה
```
  ✅ הצלחה: `pwd` מראה את תיקיית הבית שלך (למשל `/home/kali`).

**2.3** 🟢 — הצג את **כל** הקבצים בבית, כולל מוסתרים, עם פירוט וגדלים קריאים.
```bash
ls -lah
```
  👀 **חפש:** קבצים שמתחילים בנקודה (כמו `.bashrc`) — אלה המוסתרים; ועמודת הגדלים (K/M).
  ✅ הצלחה: אתה רואה גם קבצים מוסתרים עם גדלים.

**2.4** 🟡 — לאיזו מטרה משמשת כל תיקייה? `/etc/passwd` · `/usr/share/wordlists` · `/var/log` · `/root`.
  💡 רמז: README §2.2 — טבלת מבנה מערכת הקבצים.
  ✅ הצלחה: passwd=משתמשים, wordlists=רשימות סיסמאות, /var/log=לוגים, /root=בית של root.

---

## חלק ב' — קבצים, תיקיות ועריכה (🟢🟡)

**2.5** 🟢 — צור תיקייה `lab` ובתוכה קובץ ריק `notes.txt`.
```bash
mkdir lab
touch lab/notes.txt
ls -l lab
```
  ✅ הצלחה: `ls -l lab` מראה את `notes.txt`.

**2.6** 🟡 — כתוב `recon started` ל-`notes.txt`, ואז **הוסף** שורה `scanning next` בלי לדרוס.
```bash
echo "recon started" > lab/notes.txt     # > דורס/יוצר
echo "scanning next" >> lab/notes.txt     # >> מוסיף בלי לדרוס
cat lab/notes.txt
```
  👀 **חפש:** `cat` מציג **שתי** שורות.
  ✅ הצלחה: שתי השורות קיימות (שים לב ל-`>` מול `>>`).

**2.7** 🟢 — ערוך קובץ עם **nano**, ואז תרגל **יציאה מ-vim** (הפחד הכי גדול של מתחילים — ננצח אותו עכשיו!).
```bash
nano lab/notes.txt
#   הוסף שורה כלשהי, ואז שמור וצא:  Ctrl+O → Enter → Ctrl+X
cat lab/notes.txt        # ודא שהשורה שהוספת נשמרה

vim lab/notes.txt
#   כדי לצאת בלי לשמור:  לחץ Esc, הקלד  :q!  ולחץ Enter
```
  👀 **חפש:** אחרי nano — השורה שהוספת מופיעה ב-`cat`. אחרי vim — חזרת לשורת הפקודה (הצלחת לצאת!).
  ✅ הצלחה: ערכת קובץ ב-nano, ויצאת מ-vim. **זכור לכל החיים:** `Esc` → `:q!` → `Enter` (יציאה בלי שמירה) · `:wq` (שמירה ויציאה).

**2.8** 🟡 — העתק את `notes.txt` ל-`/tmp/`, ואז שנה את שם העותק ל-`backup.txt`.
```bash
cp lab/notes.txt /tmp/
mv /tmp/notes.txt /tmp/backup.txt
ls -l /tmp/backup.txt
```
  ✅ הצלחה: `/tmp/backup.txt` קיים.

**2.9** 🟡 — צור שרשרת תיקיות `project/recon/results` בפקודה אחת.
```bash
mkdir -p project/recon/results
```
  💡 רמז: הדגל `-p` יוצר את כל התיקיות בשרשרת בבת אחת.
  ✅ הצלחה: `ls -R project` מראה את כל שלוש הרמות.

---

## חלק ג' — הרשאות, משתמשים ו-sudo (🟡🟠)

**2.10** 🟡 — מי אתה? מה ה-UID שלך ובאילו קבוצות אתה?
```bash
id
```
  👀 **חפש:** `uid=`, `gid=`, ו-`groups=`.
  ✅ הצלחה: זיהית את שם המשתמש, ה-UID והקבוצות.

**2.11** 🟡 — צור `run.sh`, הפוך אותו לניתן-הרצה, ואמת ב-`ls -l`.
```bash
touch run.sh
chmod +x run.sh
ls -l run.sh
```
  👀 **חפש:** בעמודת ההרשאות מופיע עכשיו `x` (למשל `-rwxr-xr-x`).
  ✅ הצלחה: ההרשאה `x` נוספה.

**2.12** 🟠 — פענח את שורת ההרשאות `-rwxr-x---`: מה מותר לבעלים, לקבוצה, ולכולם? מה הערך ה-Octal?
  💡 רמז: README §2.6 — סעיף ההרשאות. r=4, w=2, x=1. חלק לשלישיות: בעלים/קבוצה/אחרים.
  ✅ הצלחה: בעלים=rwx(7), קבוצה=r-x(5), אחרים=---(0) → **750**.

**2.13** 🟠 — הרץ `sudo -l`. הסבר למה זו מהפקודות הראשונות של בודק חדירות אחרי שהשיג גישה.
```bash
sudo -l
```
  💡 רמז: README §2.7 — סעיף sudo. זה מראה אילו פקודות מותר להריץ כ-root.
  ✅ הצלחה: הבנת שהרשאות sudo שגויות = מסלול הסלמה ל-root (מודול 11).

---

## חלק ד' — חבילות, שירותים ורשת (🟡🟠)

**2.14** 🟡 — עדכן את רשימת החבילות והתקן את `tree`.
```bash
sudo apt update
sudo apt install tree -y
tree lab
```
  ✅ הצלחה: `tree` מותקן ומצייר את תיקיית `lab`.

**2.15** 🟡 — הפעל שרת web מקומי על פורט 8000 ואמת מטרמינל אחר.
```bash
python3 -m http.server 8000      # טרמינל 1 (משאיר רץ)
# בטרמינל 2:
curl http://127.0.0.1:8000
```
  👀 **חפש:** בטרמינל 2 מוחזר HTML; בטרמינל 1 מופיעה שורת בקשה `GET /`.
  ✅ הצלחה: השרת עונה. (עצור עם Ctrl+C.)

**2.16** 🟠 — מצא את ה-IP שלך, את ה-Gateway, ובדוק קישוריות אליו.
```bash
ip a                 # ה-IP שלך (inet תחת eth0)
ip route             # ה-Gateway (default via ...)
ping -c 3 <gateway>  # החלף בכתובת ה-Gateway
```
  ✅ הצלחה: יש לך IP + Gateway, וה-ping מקבל תשובות.

**2.17** 🟠 — הצג את כל הפורטים הפתוחים שמאזינים במכונה שלך.
```bash
ss -tulpn
```
  👀 **חפש:** עמודת `LISTEN` — כל שורה היא שירות שמאזין (למשל אם השארת את שרת ה-8000).
  ✅ הצלחה: רשימת הפורטים המאזינים.

---

## חלק ה' — חיפוש וצינורות (🟠 מתקדם)

**2.18** — מצא את כל קובצי ה-SUID במערכת, וסנן שגיאות הרשאה.
```bash
find / -perm -4000 -type f 2>/dev/null
```
  💡 רמז: `2>/dev/null` "בולע" שגיאות הרשאה כדי שהפלט יהיה נקי. זה וקטור הסלמה (מודול 11)!
  ✅ הצלחה: קיבלת רשימת קבצים עם דגל SUID.

**2.19** — כמה משתמשים מוגדרים ב-`/etc/passwd`?
```bash
cat /etc/passwd | wc -l
```
  💡 רמז: כל שורה = משתמש; `wc -l` סופר שורות.
  ✅ הצלחה: יש לך מספר.

**2.20** — הצג רק את השורות ב-`/etc/passwd` שמכילות `bash` (משתמשים עם shell פעיל).
```bash
grep bash /etc/passwd
```
  👀 **חפש:** שורות שמסתיימות ב-`/bin/bash` — אלה חשבונות אמיתיים עם shell.
  ✅ הצלחה: קיבלת רק את השורות הרלוונטיות.

---

## 🔴 אתגר מסכם — "סקריפט הסיור הראשון"

בצע ידנית **רצף סיור** על מכונת Kali שלך ותעד את הפלט של כל שלב:

1. מי אני ובאילו הרשאות (`whoami`, `id`, `sudo -l`).
2. פרטי הרשת (IP, Gateway, DNS).
3. הפורטים הפתוחים המאזינים (`ss -tulpn`).
4. חיפוש קובצי SUID (`find / -perm -4000 -type f 2>/dev/null`).
5. תוכן `/etc/passwd` — מי המשתמשים עם shell.
6. **מסקנה:** 2–3 שורות — לו היית תוקף, איזה מסלול הסלמה נראה הכי מבטיח, ולמה.

  💡 רמז: כל הפקודות כבר תרגלת אותן למעלה. רק הרץ ברצף ותעד.
  ✅ הצלחה: מסמך עם פלט של 5 השלבים + מסקנה.

> 📤 זהו למעשה "Local Enumeration" — בדיוק מה שעושים בשלב שאחרי הפריצה (מודול 11). שמור את הפלט.

---

## ✅ רשימת בקרה — לפני מעבר למודול 3
- [ ] אני מנווט בין תיקיות ומבין נתיב מוחלט מול יחסי
- [ ] אני יוצר/מעתיק/מוחק קבצים ותיקיות בביטחון
- [ ] אני עורך קבצים ב-nano (ויודע לצאת מ-vim!)
- [ ] אני קורא שורת הרשאות ומשנה אותן עם chmod
- [ ] אני מבין sudo ומדוע `sudo -l` חשוב
- [ ] אני מתקין כלים (apt / git clone) ומנהל שירותים (systemctl)
- [ ] אני משתמש ב-pipes ו-grep/find לחיפוש

*פתרונות מלאים: [`solutions.md`](solutions.md)*

---

<a id="practice"></a>
# 🧠 תרגול (Practice)

> חלק א' — **שאלות תרגול** (עם תשובות). חלק ב' — **תרחיש מהחיים האמיתיים**.

---

## חלק א' — שאלות תרגול

**Q1.** איזו פקודה מציגה את התיקייה הנוכחית? ואיזו עוברת לתיקיית הבית בשתי דרכים?

**Q2.** מה ההבדל בין `>` ל-`>>`?

**Q3.** פענח את `-rwxr-x---`: מה מותר לבעלים, לקבוצה, לכולם? מה הערך ב-Octal?

**Q4.** איזו פקודה מוצאת את כל קובצי ה-SUID במערכת, ולמה זה מעניין תוקף?

**Q5.** מה עושה `sudo -l`, ולמה זו מהפקודות הראשונות אחרי השגת גישה?

**Q6.** אילו שני קבצים ב-`/etc` הם היעדים החמים ביותר, ומה כל אחד מכיל?

**Q7.** מה עושה ה-Pipe (`|`)? תן דוגמה שסופרת כמה תהליכי apache רצים.

**Q8.** נתקעת ב-vim. כיצד יוצאים בלי לשמור?

### ✅ תשובות
1. `pwd`; לבית: `cd ~` או `cd` (וגם `cd /home/<user>`).
2. `>` דורס/יוצר קובץ; `>>` מוסיף בסוף בלי לדרוס.
3. בעלים rwx, קבוצה r-x, כולם ---; Octal = **750**.
4. `find / -perm -4000 2>/dev/null`; קובץ SUID בבעלות root רץ בהרשאות root → וקטור הסלמה.
5. מציג אילו פקודות מותר להריץ כ-root; הרשאות sudo שגויות = מסלול הסלמה קלאסי.
6. `/etc/passwd` (משתמשים, קריא לכולם), `/etc/shadow` (Hash של סיסמאות, root בלבד).
7. מעביר פלט של פקודה אחת כקלט לשנייה. `ps aux | grep apache | wc -l`.
8. `Esc` ואז `:q!` ו-Enter.

---

## חלק ב' — תרחיש מהחיים האמיתיים

### 🖥️ "Local Enumeration אחרי פריצה"
השגת Shell כמשתמש רגיל (`www-data`) על שרת Linux שנפרץ. לפני שתנסה להסלים ל-root, עליך "לסייר" במערכת. **בצע במעבדה שלך (Kali או מכונת מטרה)** את רצף הסיור, ותעד כל פלט:

```bash
# 1. מי אני ומה ההרשאות שלי
whoami; id; sudo -l
# 2. איזו מערכת זו
uname -a; cat /etc/os-release
# 3. משתמשים אנושיים
grep "bash\|sh$" /etc/passwd
# 4. וקטור הסלמה — קובצי SUID
find / -perm -4000 2>/dev/null
# 5. קבצים רגישים שאולי קריאים
ls -la /home/*; cat /etc/passwd
```

**המשימה:** לאחר ההרצה, ענה — מהם **שלושת** הממצאים שהיו הכי מבטיחים אילו היית תוקף, ולמה?

### 💡 פתרון מנחה
ממצאים מבטיחים אופייניים:
1. **`sudo -l` עם NOPASSWD** על פקודה כלשהי → הסלמה מיידית ל-root (בדוק ב-GTFOBins).
2. **קובץ SUID חריג** (למשל `/usr/bin/find` או בינארי מותאם) → ניצול להרצה כ-root.
3. **קובץ קונפיג/סיסמה קריא** ב-`/home` או `/var/www` (למשל `config.php` עם פרטי DB) → Credential Reuse.

> 🎯 זהו בדיוק מה שעושים בשלב הפוסט-אקספלויטציה (מודול 11). הרגל את הרצף הזה — הוא חוזר בכל מכונה.

[⬅️ חזרה למודול](README.md) · [תרגילים מעשיים](missions.md)

---

<a id="solutions"></a>
# ✅ פתרונות (Solutions)

> נסה לפתור לבד ב-[`missions.md`](missions.md) קודם.

---

## חלק א' — ניווט ומערכת הקבצים

**2.1**
```bash
pwd            # מציג היכן אתה (למשל /home/kali)
cd /etc
ls
```

**2.2** — שתי דרכים לחזור הביתה:
```bash
cd ~     # דרך 1: הקיצור לבית
cd       # דרך 2: cd ללא ארגומנט מחזיר תמיד הביתה
# (גם נתיב מוחלט עובד: cd /home/$USER — $USER הוא שם המשתמש שלך)
```

**2.3**
```bash
ls -lah ~     # l=פירוט, a=מוסתרים, h=גדלים קריאים
```

**2.4**
- `/etc/passwd` → תחת `/etc` (הגדרות מערכת ומשתמשים)
- `/usr/share/wordlists` → תחת `/usr` (נתונים משותפים)
- `/var/log` → תחת `/var` (לוגים)
- `/root` → תיקיית הבית של root

---

## חלק ב' — קבצים, תיקיות ועריכה

**2.5**
```bash
mkdir lab
touch lab/notes.txt
```

**2.6**
```bash
echo "recon started" > lab/notes.txt     # > דורס/יוצר
echo "scanning next" >> lab/notes.txt     # >> מוסיף בסוף
cat lab/notes.txt
# פלט:
# recon started
# scanning next
```
> ⚠️ שים לב: `>` היה דורס את השורה הראשונה; `>>` שומר עליה.

**2.7** — עריכה עם nano ויציאה מ-vim:
```bash
nano lab/notes.txt
#   הוסף שורה → Ctrl+O (שמירה) → Enter → Ctrl+X (יציאה)
cat lab/notes.txt          # ודא שהשורה נשמרה

vim lab/notes.txt
#   יציאה בלי לשמור:  Esc  ואז  :q!  ו-Enter
#   יציאה עם שמירה:   Esc  ואז  :wq  ו-Enter
#   (למצב עריכה ב-vim: מקש  i  ; לחזרה למצב פקודות: Esc)
```
> 💡 הפחד מ-vim הוא מיתוס: **Esc** מחזיר תמיד למצב פקודות, `:q!` יוצא בלי לשמור, `:wq` שומר ויוצא. זה כל הסוד.

**2.8**
```bash
cp lab/notes.txt /tmp/
mv /tmp/notes.txt /tmp/backup.txt
```

**2.9**
```bash
mkdir -p project/recon/results   # -p יוצר את כל השרשרת
```

---

## חלק ג' — הרשאות, משתמשים ו-sudo

**2.10**
```bash
id
# למשל: uid=1000(kali) gid=1000(kali) groups=1000(kali),27(sudo)...
```

**2.11**
```bash
touch run.sh
chmod +x run.sh
ls -l run.sh
# -rwxr-xr-x ... run.sh   ← ה-x מופיע
```

**2.12** — פענוח `-rwxr-x---`:
- בעלים: `rwx` = קריאה+כתיבה+הרצה
- קבוצה: `r-x` = קריאה+הרצה
- כולם: `---` = כלום
- ערך Octal: **750** (7=rwx, 5=r-x, 0=---)

**2.13**
```bash
sudo -l
```
מציג אילו פקודות מותר לך להריץ כ-root. **למה חשוב לתוקף:** אם המשתמש רשאי להריץ פקודה מסוימת כ-root (במיוחד עם `NOPASSWD`), לרוב אפשר לנצל זאת כדי להסלים ל-root — זהו אחד הבדיקות הראשונות אחרי השגת גישה.

---

## חלק ד' — חבילות, שירותים ורשת

**2.14**
```bash
sudo apt update
sudo apt install tree -y
```

**2.15**
```bash
python3 -m http.server 8000
# בטרמינל שני:
curl http://127.0.0.1:8000
# מקבלים HTML של רשימת הקבצים → השרת עונה
```

**2.16**
```bash
ip a                 # מציאת ה-IP (שורת inet תחת eth0)
ip route             # ה-Gateway (default via X.X.X.X)
ping -c 4 <gateway>  # בדיקת קישוריות
```

**2.17**
```bash
ss -tulpn
# t=TCP, u=UDP, l=מאזינים, p=תהליך, n=מספרים במקום שמות
```

---

## חלק ה' — חיפוש וצינורות

**2.18**
```bash
find / -perm -4000 -type f 2>/dev/null
# -perm -4000 = ביט SUID; 2>/dev/null מסתיר שגיאות "Permission denied"
```

**2.19**
```bash
cat /etc/passwd | wc -l
# או ישירות:
wc -l /etc/passwd
# כל שורה = משתמש אחד
```

**2.20**
```bash
grep bash /etc/passwd
# מציג משתמשים שה-shell שלהם /bin/bash (חשבונות אינטראקטיביים)
```

---

## 🔴 אתגר מסכם — פתרון לדוגמה

רצף ה-Local Enumeration:
```bash
# 1. זהות והרשאות
whoami; id; sudo -l

# 2. רשת
ip a; ip route; cat /etc/resolv.conf

# 3. פורטים מאזינים
ss -tulpn

# 4. וקטור הסלמה — קובצי SUID
find / -perm -4000 -type f 2>/dev/null

# 5. משתמשים עם shell
grep bash /etc/passwd
```

**דוגמת מסקנה:** "המשתמש שלי חבר בקבוצת `sudo`, ו-`sudo -l` הראה הרשאה להריץ `/usr/bin/find` כ-root ללא סיסמה. זהו מסלול ההסלמה המבטיח ביותר — ניתן לנצל את `find` עם `-exec` כדי להריץ פקודות כ-root (GTFOBins). לחלופין, נבדוק את קובצי ה-SUID החריגים שנמצאו."

> כיסית את כל הצעדים? מצוין — המשך ל[מודול 3](../03-networking/).

</div>
