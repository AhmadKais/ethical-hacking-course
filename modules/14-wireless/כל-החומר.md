<div dir="rtl">

# 📘 מודול 14 — תקיפות אלחוטיות (Wireless Attacks) — כל החומר בקובץ אחד

> קובץ זה מרכז את **כל** תוכן המודול לגלילה רצופה, לפי סדר הלימוד: **חומר → תרגילים → תרגול → פתרונות**.
> הקבצים הנפרדים (`README`, `missions`, `practice`, `solutions`, `slides`) עדיין קיימים לצד קובץ זה.

**ניווט מהיר:** [📖 חומר הלימוד](#material) · [🎯 תרגילים (Missions)](#missions) · [🧠 תרגול (Practice)](#practice) · [✅ פתרונות (Solutions)](#solutions)

---

<a id="material"></a>
# 📖 חומר הלימוד

> **מטרות המודול:** להבין כיצד עובדת אבטחת Wi-Fi ואיך תוקפים אותה. נלמד את משפחת הפרוטוקולים (WEP/WPA/WPA2/WPA3), נבצע **סריקת רשתות** ו-**לכידת Handshake**, נפצח סיסמאות WPA2, ונכיר מתקפות מתקדמות: **Evil Twin**, **Deauthentication**, ו-**WPS**. המודול מקנה את היכולת לבצע בדיקת אבטחה של רשת אלחוטית ארגונית.
>
> **קבצים:** `README.md` · [`missions.md`](missions.md) · [`solutions.md`](solutions.md) · [`practice.md`](practice.md) · [`slides.md`](slides.md).

> ⚠️ **דרישת חומרה — קרא לפני שתתחיל!**
> תקיפות אלחוטיות דורשות **מתאם Wi-Fi חיצוני (USB) שתומך ב-Monitor Mode ו-Packet Injection** (למשל שבבי Atheros AR9271, Ralink RT3070, Realtek RTL8812AU). **הכרטיס המובנה בלפטופ לרוב לא מספיק**, וגם **לא ניתן לבצע זאת ב-VM ללא חיבור USB passthrough של מתאם תואם**. אם אין חומרה — הבן את התיאוריה מהמודול, ותרגל את **הפיצוח** (החלק החישובי) על קבצי `.cap` להורדה. הפעולות האחרות דורשות מתאם פיזי.

---

## תוכן העניינים
1. [יסודות רשתות אלחוטיות](#141-יסודות-רשתות-אלחוטיות)
2. [פרוטוקולי אבטחה: WEP → WPA3](#142-פרוטוקולי-אבטחה-wep--wpa3)
3. [ציוד וסביבה (Monitor Mode)](#143-ציוד-וסביבה-monitor-mode)
4. [סריקה ואיסוף מידע](#144-סריקה-ואיסוף-מידע)
5. [Deauthentication ולכידת Handshake](#145-deauthentication-ולכידת-handshake)
6. [פיצוח WPA2](#146-פיצוח-wpa2)
7. [מתקפות מתקדמות: Evil Twin ו-WPS](#147-מתקפות-מתקדמות-evil-twin-ו-wps)
8. [הגנה על רשת אלחוטית](#148-הגנה-על-רשת-אלחוטית)

---

## 14.1 יסודות רשתות אלחוטיות

- **SSID** — שם הרשת המשודר.
- **BSSID** — כתובת ה-MAC של נקודת הגישה (Access Point / AP).
- **Client (STA)** — מכשיר מחובר (טלפון, לפטופ).
- **ערוצים (Channels)** — תדרים (2.4GHz / 5GHz) שבהם משדרים.
- **Beacon Frames** — ה-AP משדר את נוכחותו ברציפות.

תעבורה אלחוטית **משודרת באוויר** — כל מי שבטווח יכול "להאזין" (בניגוד לרשת קווית). לכן ההצפנה קריטית.

> 🎯 הרעיון המרכזי: אם אנחנו יכולים ללכוד את חבילות ה-**Handshake** (רגע ההתחברות), נוכל לנסות לפצח את הסיסמה offline — בדיוק כמו hashes (מודול 8).

---

## 14.2 פרוטוקולי אבטחה: WEP → WPA3

| פרוטוקול | שנה | מצב אבטחה |
|----------|-----|-----------|
| **WEP** | 1997 | **שבור לחלוטין** — נפצח תוך דקות (חולשת IV) |
| **WPA** | 2003 | חלש (TKIP); פתרון ביניים |
| **WPA2** | 2004 | **הנפוץ ביותר**; פגיע לפיצוח Handshake (סיסמה חלשה) |
| **WPA3** | 2018 | חזק (SAE); עמיד לפיצוח offline, אך מתקפות עדיין קיימות |

**המיקוד המעשי:** רוב הרשתות עדיין **WPA2-PSK**. החולשה אינה בפרוטוקול עצמו אלא ב**סיסמה חלשה** — לוכדים Handshake ומפצחים במילון.

> 🔍 WEP הוא שריד היסטורי — אם תמצא אותו בבדיקה, זה ממצא קריטי מיידי. WPA2 בטוח *כל עוד הסיסמה חזקה*.

---

## 14.3 ציוד וסביבה (Monitor Mode)

**Monitor Mode** מאפשר למתאם ל"האזין" לכל התעבורה באוויר (לא רק לרשת שאליה מחובר). **Packet Injection** מאפשר לשלוח חבילות (נדרש ל-Deauth).

```bash
# זיהוי המתאם:
iwconfig                          # מחפשים wlan0

# הפעלת Monitor Mode (חבילת aircrack-ng):
sudo airmon-ng check kill         # עוצר תהליכים מפריעים
sudo airmon-ng start wlan0        # → wlan0mon במצב monitor

# אימות:
iwconfig wlan0mon                 # Mode:Monitor
```

> ⚠️ אם `airmon-ng` לא מציג את הכרטיס במצב monitor — הכרטיס/הדרייבר לא תומך. זו הסיבה שצריך מתאם USB ייעודי.

---

## 14.4 סריקה ואיסוף מידע

**airodump-ng** סורק את כל הרשתות והלקוחות בסביבה:

```bash
sudo airodump-ng wlan0mon
```
פלט טיפוסי:
```text
 BSSID              PWR  CH  ENC   CIPHER  AUTH  ESSID
 AA:BB:CC:11:22:33  -40   6  WPA2  CCMP    PSK   HomeNet
 DD:EE:FF:44:55:66  -70  11  WPA2  CCMP    PSK   Office-WiFi
```
נבחר יעד (BSSID + ערוץ) ונתמקד בו כדי ללכוד Handshake:
```bash
sudo airodump-ng --bssid AA:BB:CC:11:22:33 -c 6 -w capture wlan0mon
# --bssid = היעד, -c = ערוץ, -w = שמירה לקובץ
```

> 🔍 עמודת **PWR** = עוצמת אות (קרוב יותר = מספר גבוה יותר, שלילי פחות). עמודת **STATION** (בתחתית) מראה לקוחות מחוברים — נזדקק להם ל-Deauth.

---

## 14.5 Deauthentication ולכידת Handshake

ה-**4-Way Handshake** נלכד רק כשלקוח **מתחבר** ל-AP. במקום לחכות — נכריח לקוח קיים להתנתק (Deauth), והוא יתחבר מחדש אוטומטית → לוכדים.

```bash
# בזמן ש-airodump-ng רץ על היעד, בטרמינל שני:
sudo aireplay-ng --deauth 5 -a AA:BB:CC:11:22:33 -c <client-mac> wlan0mon
# --deauth 5 = שלח 5 חבילות ניתוק; -a = ה-AP; -c = הלקוח
```
כשהלקוח מתחבר מחדש, airodump-ng יציג למעלה: **`WPA handshake: AA:BB:CC:11:22:33`** ✅ — לכדנו.

> 🎯 Deauth היא גם מתקפת **DoS** בפני עצמה (ניתוק מתמשך), אך כאן המטרה היא רק לכידת ה-Handshake. שלח מעט חבילות — לא צריך להטריד את הרשת.

---

## 14.6 פיצוח WPA2

ה-Handshake שנלכד מכיל את הסיסמה בצורה מוצפנת (PMK). מפצחים **offline** במילון:

```bash
# aircrack-ng:
sudo aircrack-ng capture-01.cap -w /usr/share/wordlists/rockyou.txt

# מהיר יותר עם hashcat (ממירים תחילה ל-hc22000):
hcxpcapngtool -o hash.hc22000 capture-01.cap
hashcat -m 22000 hash.hc22000 /usr/share/wordlists/rockyou.txt
```
אם הסיסמה במילון — נקבל אותה. אם היא **חזקה** (ארוכה, אקראית) — הפיצוח ייכשל, וזה בדיוק מדוע WPA2 בטוח עם סיסמה טובה.

> 🔑 **החיבור למודול 8:** זהו בדיוק אותו רעיון של פיצוח סיסמאות offline — לוכדים "hash" (כאן: Handshake) ומפעילים מילון/hashcat. WPA2 חזק = סיסמה שלא נמצאת בשום wordlist.

---

## 14.7 מתקפות מתקדמות: Evil Twin ו-WPS

### Evil Twin (תאום מרושע)
מקימים **AP מזויף** עם אותו SSID של הרשת האמיתית. הקורבן מתחבר בטעות, ואנחנו יכולים ללכוד תעבורה או להציג **דף התחברות מזויף** (Captive Portal) שגונב את הסיסמה.
```bash
# כלים: airgeddon, wifiphisher — מאוטמים את כל התהליך
```

### WPS Attacks
**WPS** (הכפתור/PIN לחיבור קל) פגיע למתקפת **brute-force** על ה-PIN (8 ספרות):
```bash
sudo reaver -i wlan0mon -b <BSSID> -vv
# או wash לזיהוי רשתות עם WPS פעיל:
sudo wash -i wlan0mon
```

> 💡 **Evil Twin** מסוכן במיוחד כי הוא עוקף הצפנה חזקה — במקום לפצח את WPA2, פשוט **מרמים את המשתמש** למסור את הסיסמה. הנדסה חברתית (מודול 15) פוגשת אלחוט.

---

## 14.8 הגנה על רשת אלחוטית

להמלצות בדוח (מודול 16):

- **סיסמה חזקה וארוכה** (15+ תווים אקראיים) — ההגנה מספר 1 מול פיצוח WPA2.
- **WPA3** במקום WPA2 היכן שאפשר.
- **כבה WPS** — וקטור brute-force מיותר.
- **הפרד רשת אורחים** מהרשת הארגונית (VLAN).
- **WPA2-Enterprise (802.1X / RADIUS)** — אימות פר-משתמש במקום סיסמה משותפת.
- **נטר AP מזויפים** (Rogue AP Detection) מול Evil Twin.

---

## סיכום המודול

- תעבורה אלחוטית משודרת באוויר → הצפנה קריטית; **WEP שבור**, **WPA2** הנפוץ, **WPA3** החזק.
- דורש מתאם עם **Monitor Mode + Injection**; מפעילים עם `airmon-ng`.
- **airodump-ng** סורק; **aireplay-ng** מבצע Deauth ללכידת **Handshake**.
- מפצחים WPA2 **offline** (aircrack-ng / hashcat -m 22000) — בדיוק כמו מודול 8.
- **Evil Twin** ו-**WPS** הן מתקפות מתקדמות; Evil Twin עוקף הצפנה דרך הטעיה.
- **הגנה:** סיסמה חזקה, WPA3, כיבוי WPS, WPA2-Enterprise.

### מה הלאה?
➡️ [**תרגילים — `missions.md`**](missions.md) · [**פתרונות — `solutions.md`**](solutions.md) · [**תרגול ותרחישים — `practice.md`**](practice.md)
➡️ [**מודול 15 — הנדסה חברתית ופישינג**](../15-social-engineering/)

[⬅️ חזרה למפת הקורס](../../README.md)

---

<a id="missions"></a>
# 🎯 תרגילים (Missions)

> ⚠️ **רק על רשת אלחוטית שבבעלותך או שקיבלת עליה אישור בכתב.** האזנה/תקיפה של רשת זרה = עבירה פלילית. חלק מהתרגילים דורשים **מתאם Wi-Fi עם Monitor Mode**; מי שאין לו — יבצע את תרגילי הפיצוח (על קובצי `.cap`) והתיאוריה. פתרונות ב-[**`solutions.md`**](solutions.md).

> ### 🚦 לפני שמתחילים — קרא אותי!
> - **מה צריך:** **Kali** + **מתאם Wi-Fi USB שתומך ב-Monitor Mode ו-Injection** (הכרטיס המובנה לרוב לא מספיק, וגם לא עובד ב-VM ללא USB passthrough). **אין חומרה?** תרגל את התיאוריה ואת תרגילי ה**פיצוח** על קובץ `.cap` להורדה.
> - **איך עובדים:** קרא קודם את ה-[`README.md`](README.md). למשימות עם פקודה — **הפקודה מופיעה** + מה לחפש; לרעיוניות **💡 רמז**.
> - **נתקעת?** אם `airmon-ng` לא נכנס ל-monitor mode — הכרטיס/דרייבר לא תומך. נסה, ואז הצץ ב-[`solutions.md`](solutions.md).
> - **טיפ:** התרגיל הכי חשוב — בדוק את הרשת שלך בבית. רוב האנשים מגלים שהסיסמה שלהם ב-rockyou.

מקרא רמות: 🟢 קל · 🟡 בינוני · 🟠 מתקדם · 🔴 אתגר

---

## חלק א' — תיאוריה וסביבה (🟢)

**14.1** — הסבר את ההבדל בין SSID, BSSID ו-Client. מהו Beacon Frame?
  💡 רמז: README §14.1.
  ✅ הצלחה: SSID=שם, BSSID=MAC של ה-AP, Client=מכשיר מחובר, Beacon=שידור נוכחות.

**14.2** — סדר את WEP, WPA, WPA2, WPA3 מהחלש לחזק. מדוע WEP "שבור"?
  💡 רמז: README §14.2.
  ✅ הצלחה: הסדר נכון + הסבר חולשת ה-IV של WEP.

**14.3** 🟢 — הפעל **Monitor Mode** ואמת.
```bash
sudo airmon-ng check kill
sudo airmon-ng start wlan0
iwconfig wlan0mon
```
  👀 **חפש:** `Mode:Monitor` בפלט של `iwconfig`.
  ✅ הצלחה: המתאם במצב Monitor (`wlan0mon`).

---

## חלק ב' — סריקה ולכידה (🟡🟠)

**14.4** 🟡 — סרוק את הרשתות בסביבתך וזהה את **הרשת שלך**.
```bash
sudo airodump-ng wlan0mon
```
  👀 **חפש:** בטבלה — BSSID, CH (ערוץ), ENC (WPA2), ESSID של הרשת שלך. רשום אותם.
  ✅ הצלחה: יש לך BSSID + ערוץ + הצפנה של הרשת שלך.

**14.5** 🟠 — התמקד ברשת שלך ולכוד **Handshake**.
```bash
sudo airodump-ng --bssid <BSSID> -c <ערוץ> -w capture wlan0mon
```
  👀 **חפש:** למעלה מופיע `WPA handshake: <BSSID>` — סימן שלכדת.
  ✅ הצלחה: לכדת את ה-Handshake לקובץ.

**14.6** 🟠 — בצע **Deauth** על מכשיר שלך כדי לזרז את הלכידה.
```bash
sudo aireplay-ng --deauth 5 -a <BSSID> -c <client-mac> wlan0mon
```
  💡 רמז: README §14.5. מעט חבילות (5) מספיקות — המטרה חיבור מחדש אחד, לא DoS.
  ✅ הצלחה: המכשיר התנתק והתחבר מחדש, וה-Handshake נלכד.

---

## חלק ג' — פיצוח ומתקדם (🟠)

**14.7** 🟠 — פצח את ה-Handshake.
```bash
sudo aircrack-ng capture-01.cap -w /usr/share/wordlists/rockyou.txt
# או hashcat:
hcxpcapngtool -o hash.hc22000 capture-01.cap
hashcat -m 22000 hash.hc22000 /usr/share/wordlists/rockyou.txt
```
  👀 **חפש:** `KEY FOUND! [ הסיסמה ]` — אם הסיסמה במילון.
  ✅ הצלחה: פיצחת (או קבעת שהסיסמה חזקה מדי — וזה טוב!).

**14.8** 🟠 — זהה רשת עם **WPS** פעיל. למה WPS וקטור תקיפה?
```bash
sudo wash -i wlan0mon
```
  💡 רמז: README §14.7. ה-PIN בן 8 ספרות → brute-force עם reaver.
  ✅ הצלחה: זיהית אם WPS פעיל והסברת את הסיכון.

**14.9** 🟠 — הסבר כיצד **Evil Twin** מצליח גם מול WPA2 חזק.
  💡 רמז: README §14.7. הוא לא תוקף הצפנה — מרמה את המשתמש.
  ✅ הצלחה: הסברת שהוא עוקף הצפנה דרך הטעיה (הנדסה חברתית).

---

## 🔴 אתגר מסכם — "בדיקת אבטחת ה-Wi-Fi הביתי"

על **הרשת האלחוטית שלך בבית** (בעלות מלאה):

1. סרוק וזהה את פרטי הרשת (BSSID, ערוץ, הצפנה).
2. לכוד Handshake (עם Deauth של מכשיר שלך).
3. נסה לפצח עם rockyou. **האם הסיסמה שלך שרדה?**
4. בדוק אם WPS פעיל.
5. **תעד דוח:** ממצאים (WPS? חוזק סיסמה?), והמלצות (WPA3, סיסמה חזקה, כיבוי WPS).

  💡 רמז: כל השלבים בתרגילים 14.3–14.8. מבנה דוח ב-[`solutions.md`](solutions.md).
  ✅ הצלחה: דוח על הרשת שלך + המלצות שיפור.

> 📤 בדיקה זו על הרשת שלך בטוחה, חוקית וחושפת — אם הסיסמה שלך נמצאה, שדרג אותה מיד.

---

## ✅ רשימת בקרה — לפני מעבר למודול 15
- [ ] אני מבין SSID/BSSID/Client ואת הבדלי WEP→WPA3
- [ ] אני מפעיל Monitor Mode
- [ ] אני סורק רשתות עם airodump-ng
- [ ] אני מבצע Deauth ולוכד Handshake
- [ ] אני מפצח WPA2 עם aircrack-ng/hashcat
- [ ] אני מבין WPS ו-Evil Twin
- [ ] אני יודע להמליץ הגנות (סיסמה חזקה, WPA3, כיבוי WPS)

*פתרונות מלאים: [`solutions.md`](solutions.md)*

---

<a id="practice"></a>
# 🧠 תרגול (Practice)

> חלק א' — **שאלות תרגול** (עם תשובות). חלק ב' — **תרחיש מהחיים האמיתיים**.

---

## חלק א' — שאלות תרגול

**Q1.** למה הצפנה קריטית ברשת אלחוטית יותר מאשר בקווית?

**Q2.** מדוע WEP נחשב "שבור", ומהו הפרוטוקול הנפוץ היום?

**Q3.** מהו Monitor Mode, ולמה צריך מתאם מיוחד?

**Q4.** מהו 4-Way Handshake, ולמה משתמשים ב-Deauth כדי ללכוד אותו?

**Q5.** למה פיצוח WPA2 מזכיר את מודול 8 (פיצוח סיסמאות)?

**Q6.** מהי מתקפת Evil Twin, ולמה היא עוקפת WPA2 חזק?

**Q7.** מהו WPS ולמה הוא וקטור תקיפה?

**Q8.** מהי ההגנה מספר 1 מול פיצוח WPA2?

### ✅ תשובות
1. התעבורה משודרת באוויר — כל מי שבטווח יכול להאזין; ברשת קווית צריך גישה פיזית לכבל.
2. חולשת ה-IV מאפשרת פיצוח תוך דקות ללא קשר לסיסמה; הנפוץ היום = **WPA2-PSK**.
3. מצב שמאפשר האזנה לכל התעבורה באוויר; דורש מתאם/דרייבר שתומכים בו + Packet Injection.
4. חילופי המפתחות בעת התחברות לקוח ל-AP; Deauth מנתק לקוח קיים שיתחבר מחדש → מזרז לכידה.
5. גם כאן לוכדים "hash" (Handshake) ומפצחים **offline** במילון/hashcat — אותו עיקרון בדיוק.
6. AP מזויף עם אותו SSID + דף התחברות מזויף; עוקף הצפנה כי **מרמים את המשתמש** למסור סיסמה, לא מפצחים.
7. חיבור קל ב-PIN/כפתור; ה-PIN בן 8 ספרות ניתן ל-brute-force (reaver) ופורץ WPA2.
8. **סיסמה חזקה וארוכה** (15+ תווים אקראיים) שלא נמצאת בשום wordlist.

---

## חלק ב' — תרחיש מהחיים האמיתי

### 📡 "בדיקת אבטחה אלחוטית בבית קפה של הלקוח"
לקוח (רשת בתי קפה) ביקש בדיקת אבטחה של ה-Wi-Fi בסניפים — **עם חוזה והרשאה בכתב**. גילית שתי בעיות.

```text
1. הרשת "CoffeeShop-Staff" (WPA2) — לכדת Handshake, פיצחת:
   הסיסמה "coffee2020" (במילון!) → גישה מלאה לרשת העובדים.
2. רשת האורחים והעובדים על אותו VLAN → מהאורחים
   מגיעים לקופה ולמצלמות.
3. WPS פעיל בכל הראוטרים → וקטור נוסף.
4. הדגמת Evil Twin: הקמת "CoffeeShop-Free" מזויף →
   3 לקוחות התחברו והזינו פרטים בדף מזויף.
```

**המשימה:**
1. איזה מהממצאים הוא **החמור ביותר** לעסק, ולמה?
2. מדוע ה-Evil Twin עבד למרות שרשת העובדים מוצפנת WPA2?
3. כתוב **שלוש** המלצות תיקון בסדר עדיפות.

### 💡 פתרון מנחה
1. **הרשת השטוחה (אין הפרדת VLAN)** — סיסמה חלשה נותנת גישה, אבל היעדר סגמנטציה אומר שמהרשת מגיעים ל**קופה ולמצלמות** = חשיפת נתוני תשלום ולקוחות. זה הסיכון העסקי הגדול.
2. Evil Twin לא תוקף את ההצפנה — הוא יוצר רשת חדשה ומרמה משתמשים; ההגנה של WPA2 לא רלוונטית כי המשתמש **מוסר את הסיסמה מרצונו** לדף המזויף.
3. (א) **הפרדת VLAN** בין אורחים/עובדים/מערכות תשלום — מונע את הנזק הגדול; (ב) **סיסמאות חזקות + WPA2-Enterprise** לרשת העובדים; (ג) **כיבוי WPS** + ניטור Rogue AP מול Evil Twin.

> 🎯 בבדיקה אלחוטית, לרוב הבעיה החמורה אינה ההצפנה אלא ה**ארכיטקטורה** (רשת שטוחה) וה**גורם האנושי** (Evil Twin). כאן אלחוט פוגש הנדסה חברתית — מודול 15.

[⬅️ חזרה למודול](README.md) · [תרגילים מעשיים](missions.md)

---

<a id="solutions"></a>
# ✅ פתרונות (Solutions)

> נסה לבד ב-[`missions.md`](missions.md) קודם. **רק על רשת שבבעלותך.**

---

## חלק א' — תיאוריה וסביבה

**14.1** — **SSID** = שם הרשת; **BSSID** = כתובת MAC של ה-AP; **Client** = מכשיר מחובר. **Beacon** = מסגרת שה-AP משדר ברציפות להכריז על נוכחותו.

**14.2** — WEP < WPA < WPA2 < WPA3. WEP "שבור" בגלל חולשת ה-**IV** (Initialization Vector) — נפצח תוך דקות ללא קשר לחוזק הסיסמה.

**14.3** — Monitor Mode מאפשר האזנה לכל התעבורה באוויר (לא רק לרשת מחוברת).
```bash
sudo airmon-ng check kill
sudo airmon-ng start wlan0        # → wlan0mon
iwconfig wlan0mon                 # Mode:Monitor ✅
```

---

## חלק ב' — סריקה ולכידה

**14.4**
```bash
sudo airodump-ng wlan0mon
# מזהים בטבלה: BSSID, CH, ENC (WPA2), ESSID של הרשת שלך
```

**14.5**
```bash
sudo airodump-ng --bssid AA:BB:CC:11:22:33 -c 6 -w capture wlan0mon
```
מצליחים כשמופיע למעלה: **`WPA handshake: AA:BB:CC:11:22:33`**.

**14.6**
```bash
sudo aireplay-ng --deauth 5 -a AA:BB:CC:11:22:33 -c <my-client> wlan0mon
```
מעט חבילות (5) מספיקות כי מטרתנו רק לגרום לניתוק+חיבור מחדש אחד ללכידת ה-Handshake — לא DoS.

---

## חלק ג' — פיצוח ומתקדם

**14.7**
```bash
sudo aircrack-ng capture-01.cap -w /usr/share/wordlists/rockyou.txt
# או hashcat:
hcxpcapngtool -o hash.hc22000 capture-01.cap
hashcat -m 22000 hash.hc22000 /usr/share/wordlists/rockyou.txt
```
אם הסיסמה במילון — תוצג; אם חזקה — הפיצוח נכשל (וזה טוב).

**14.8**
```bash
sudo wash -i wlan0mon
```
WPS פגיע כי ה-PIN בן 8 ספרות ניתן ל-brute-force (reaver) — פורץ את WPA2 בלי לפצח את הסיסמה עצמה.

**14.9** — **Evil Twin**: מקימים AP מזויף עם אותו SSID; הקורבן מתחבר בטעות ומקבל דף התחברות מזויף שגונב את הסיסמה. מסוכן כי הוא **עוקף את ההצפנה לגמרי** — לא מפצחים WPA2, אלא מרמים את המשתמש (הנדסה חברתית).

---

## 🔴 אתגר מסכם — מבנה דוח

```markdown
# דוח בדיקת Wi-Fi ביתי

## ממצאים
- הצפנה: WPA2-PSK (CCMP)
- WPS: פעיל ← סיכון (brute-force אפשרי)
- סיסמה: נמצאה ב-rockyou תוך 3 דקות ← קריטי!

## המלצות
1. החלף סיסמה ל-15+ תווים אקראיים (לא במילון)
2. כבה WPS בממשק הראוטר
3. שדרג ל-WPA3 אם נתמך
4. הפרד רשת אורחים
```

> עברת? בדקת רשת אמיתית באופן חוקי. המשך ל[מודול 15](../15-social-engineering/).

</div>
