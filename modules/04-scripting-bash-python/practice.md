<div dir="rtl">

# 🧠 תרגול — מודול 4: סקריפטינג (Bash ו-Python)

> חלק א' — **שאלות תרגול** (עם תשובות). חלק ב' — **תרחיש מהחיים האמיתיים**.

---

## חלק א' — שאלות תרגול

**Q1.** מהו ה-Shebang, ולמה הוא חובה בתחילת סקריפט Bash?

**Q2.** מה ההבדל בין `$1` ל-`read` בקבלת קלט?

**Q3.** ב-Python, איזה מבנה נתונים אינו ניתן לשינוי: List, Dictionary, או Tuple?

**Q4.** מה מחזירה `socket.connect_ex()` כשהפורט פתוח?

**Q5.** נתון `banner = "nginx/1.18.0"`. כתוב שורת Python המחלצת רק `1.18.0`.

**Q6.** מתי תעדיף Bash ומתי Python?

**Q7.** מה עושה `s.settimeout(0.5)` בסורק הפורטים, ולמה זה חשוב?

**Q8.** כתוב לולאת Bash שמדפיסה `Host 10.0.0.X` עבור X מ-1 עד 5.

### ✅ תשובות
1. `#!/bin/bash` — אומר למערכת עם איזה מפרש להריץ את הסקריפט.
2. `$1` = ארגומנט משורת הפקודה (בזמן ההרצה); `read` = קלט אינטראקטיבי תוך כדי ריצה.
3. **Tuple** (`( )`) אינו ניתן לשינוי.
4. **0** (אפס).
5. `banner.split("/")[1]`.
6. Bash — אוטומציה של פקודות מערכת/one-liners; Python — כלים, רשת ולוגיקה מורכבת.
7. מגביל את זמן ההמתנה לחיבור; מונע היתקעות על פורט מסונן ומאיץ את הסריקה.
8. `for i in $(seq 1 5); do echo "Host 10.0.0.$i"; done`

---

## חלק ב' — תרחיש מהחיים האמיתיים

### ⚙️ "כתוב כלי אוטומציה אמיתי"
בבדיקה יש לך רשימת יעדים ואתה רוצה לזהות מהר אילו מהם מריצים שרת web על פורט 80. **כתוב והרץ במעבדה** סקריפט Python שעושה זאת:

**דרישות:**
1. רשימת יעדים (List) — למשל `["127.0.0.1", "192.168.1.1"]`.
2. עבור כל יעד, בדוק אם פורט **80** פתוח (בעזרת `socket`).
3. הדפס `[+] <ip> has web server` רק ליעדים שבהם 80 פתוח.
4. טפל בשגיאות (timeout / כתובת לא תקינה).

**רמז:** שלב את `connect_ex`, לולאת `for`, ותנאי `if`.

### 💡 פתרון מנחה
```python
#!/usr/bin/env python3
import socket

targets = ["127.0.0.1", "192.168.1.1"]   # התאם למעבדה שלך
port = 80

for ip in targets:
    try:
        s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        s.settimeout(0.5)
        if s.connect_ex((ip, port)) == 0:
            print(f"[+] {ip} has web server (port 80 open)")
        s.close()
    except socket.error:
        print(f"[!] {ip} — error")
```
**בדיקה:** הפעל שרת מקומי `python3 -m http.server 80` (כ-root) ותראה את `127.0.0.1` מזוהה.

> 🎯 זהו למעשה סורק ממוקד-שירות — אותו רעיון של nmap, אך משלך. הבנת הקוד הזה = הבנת איך כלי סריקה עובדים מבפנים.

[⬅️ חזרה למודול](README.md) · [תרגילים מעשיים](missions.md)

</div>
