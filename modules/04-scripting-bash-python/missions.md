<div dir="rtl">

# 🎯 תרגילים — מודול 4: סקריפטינג (Bash ו-Python)

> כל הקוד רץ במעבדה שלך. סריקות — **רק** על `127.0.0.1` או מכונות המעבדה. פתרונות ב-[**`solutions.md`**](solutions.md).

מקרא רמות: 🟢 קל · 🟡 בינוני · 🟠 מתקדם · 🔴 אתגר

---

## חלק א' — Bash (🟢🟡)

**4.1** 🟢 — כתוב סקריפט `hello.sh` עם Shebang שמדפיס "Hello Hacker", הפוך אותו לניתן-הרצה, והרץ.

**4.2** 🟢 — כתוב סקריפט שמקבל שם באמצעות `read` ומדפיס `Welcome, <name>`.

**4.3** 🟡 — כתוב סקריפט שמקבל מספר פורט כארגומנט (`$1`) ומדפיס אם הוא 80 (HTTP), 443 (HTTPS), או Unknown.

**4.4** 🟡 — כתוב לולאה ב-Bash שמדפיסה `Pinging 10.0.0.X` עבור X מ-1 עד 5.

---

## חלק ב' — Python בסיסי (🟢🟡)

**4.5** 🟢 — כתוב סקריפט Python שמגדיר משתנה `target` ומדפיס אותו עם f-string.

**4.6** 🟡 — נתון `banner = "Apache/2.4.49 (Debian)"`. חלץ והדפס **רק** את מספר הגרסה (`2.4.49`).

**4.7** 🟡 — צור מילון `services` הממפה 21,22,80,443 לשמות השירותים, ובלולאה הדפס `Port X -> Service`.

---

## חלק ג' — לוגיקה ולולאות (🟠)

**4.8** — כתוב פונקציה `check_port(port)` שמחזירה את שם השירות אם הפורט מוכר (מתוך מילון), אחרת `"Unknown"`.

**4.9** — כתוב לולאה שעוברת על פורטים 20–25 ומדפיסה לכל אחד את שם השירות (בעזרת הפונקציה מ-4.8).

---

## חלק ד' — הכלים האמיתיים (🟠 מתקדם)

**4.10** — הרץ את **Ping Sweep** (מהחומר) על תת-הרשת של המעבדה שלך. כמה מכונות חיות נמצאו?

**4.11** — הרץ את **Port Scanner** (מהחומר) על `127.0.0.1`. אילו פורטים פתוחים? (רמז: הפעל שירות עם `python3 -m http.server 8000` כדי לראות פורט נפתח).

**4.12** — קרא את קוד ה-Port Scanner וענה: מה מחזירה `connect_ex` בפורט פתוח? מה תפקיד `settimeout`? מדוע צריך את `try/except KeyboardInterrupt`?

---

## 🔴 אתגר מסכם — "שדרוג הסורק"

שדרג את ה-Port Scanner כך שיכלול:

1. **שם שירות** — לצד כל פורט פתוח, הדפס את שם השירות ממילון `services`.
2. **טווח מהמשתמש** — קבל את פורט ההתחלה והסיום מהמשתמש (`input`).
3. **סיכום** — בסוף: `[*] Scan complete. Found N open ports.`
4. **זמן** — מדוד את משך הסריקה עם המודול `time`.
5. **(בונוס Bash)** — כתוב סקריפט Bash שמריץ קודם את ה-Ping Sweep, ואז מריץ את ה-Port Scanner על כל מכונה חיה שנמצאה.

**פלט רצוי לדוגמה:**
```
[*] Enter target: 127.0.0.1
[*] Ports 1-1024
[+] Port 80   is OPEN (HTTP)
[+] Port 8000 is OPEN
[*] Scan complete. Found 2 open ports in 2.3s
```

> 📤 בניית שני הכלים האלה = יסוד המעבר לשלב הסריקה (מודול 6), שם נשתמש ב-nmap המקצועי.

---

## ✅ רשימת בקרה — לפני מעבר למודול 5
- [ ] אני כותב ומריץ סקריפט Bash עם Shebang, משתנים, תנאים ולולאות
- [ ] בניתי והרצתי את ה-Ping Sweep
- [ ] אני מבין טיפוסים ומבני נתונים ב-Python
- [ ] אני כותב פונקציות ולולאות ב-Python
- [ ] בניתי והרצתי את ה-Port Scanner והבנתי את הקוד
- [ ] אני יודע מתי להשתמש ב-Bash ומתי ב-Python

*פתרונות מלאים: [`solutions.md`](solutions.md)*

</div>
