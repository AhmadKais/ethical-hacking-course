<div dir="rtl">

# 📘 מודול 3 — יסודות רשתות (Networking Fundamentals) — כל החומר בקובץ אחד

> קובץ זה מרכז את **כל** תוכן המודול לגלילה רצופה, לפי סדר הלימוד: **חומר → תרגילים → תרגול → פתרונות**.
> הקבצים הנפרדים (`README`, `missions`, `practice`, `solutions`, `slides`) עדיין קיימים לצד קובץ זה.

**ניווט מהיר:** [📖 חומר הלימוד](#material) · [🎯 תרגילים (Missions)](#missions) · [🧠 תרגול (Practice)](#practice) · [✅ פתרונות (Solutions)](#solutions)

---

<a id="material"></a>
# 📖 חומר הלימוד

> **מטרות המודול:** להבין לעומק כיצד מחשבים מתקשרים ברשת — מודל OSI ו-TCP/IP, TCP מול UDP ולחיצת היד, כתובות IP (IPv4/IPv6), MAC ו-ARP, פורטים ופרוטוקולים, Subnetting, ופרוטוקולי התשתית (DNS, DHCP, NAT, ICMP). כל זה עם דוגמאות מעשיות ופקודות.
>
> **קבצים:** `README.md` · [`missions.md`](missions.md) · [`solutions.md`](solutions.md) · [`practice.md`](practice.md) · [`slides.md`](slides.md).
>
> **מבוסס על שיעור המקור** #07 ("Networking — 3 hours").

> **למה זה חשוב להאקר?** כל תקיפה עוברת דרך הרשת. בלי להבין פורטים, פרוטוקולים, כתובות ולחיצת יד — אי אפשר להבין מה סורקים, מה מוצאים, וכיצד מנצלים. זה הבסיס למודולים 5–6 (סריקה וניצול).

---

## תוכן העניינים
1. [מהי רשת ומסע החבילה](#31-מהי-רשת-ומסע-החבילה)
2. [מודל OSI](#32-מודל-osi)
3. [מודל TCP/IP](#33-מודל-tcpip)
4. [TCP מול UDP ולחיצת היד](#34-tcp-מול-udp-ולחיצת-היד)
5. [כתובות IP](#35-כתובות-ip)
6. [MAC ו-ARP](#36-mac-ו-arp)
7. [פורטים ופרוטוקולים](#37-פורטים-ופרוטוקולים-נפוצים)
8. [Subnetting](#38-subnetting-חלוקת-רשת)
9. [פרוטוקולי תשתית: DNS, DHCP, NAT](#39-פרוטוקולי-תשתית)
10. [כלים מעשיים](#310-כלים-מעשיים)

---

## 3.1 מהי רשת ומסע החבילה

**רשת** היא אוסף מכשירים המתקשרים ביניהם. המידע נשלח ב**חבילות (Packets)** — יחידות קטנות של נתונים העוברות מהמקור ליעד, לעיתים דרך צמתים רבים (Routers).

### מסע חבילה בקצרה — מה קורה כשאתה גולש ל-`example.com`
1. **DNS:** המחשב שואל את שרת ה-DNS "מה הכתובת של `example.com`?", ומקבל בחזרה כתובת `IP` — למשל `93.184.216.34`.
2. **ARP:** כדי לשלוח ברשת המקומית, המחשב מוצא את כתובת ה-`MAC` של ה-Gateway.
3. **TCP Handshake:** נפתח חיבור לפורט `80` או `443` (הרצף `SYN → SYN-ACK → ACK`).
4. **HTTP:** נשלחת בקשה `GET /`, והשרת מחזיר את הדף.

> 💡 כל שלב כאן שייך לשכבה אחרת במודל OSI — וכל שלב הוא גם **נקודת תקיפה אפשרית**: זיוף `DNS`, הרעלת `ARP`, סריקת פורטים, וחולשות Web.

---

## 3.2 מודל OSI

מודל **OSI** מתאר תקשורת בשבע שכבות. כל שכבה מדברת עם המקבילה בצד השני, ומוסיפה/מסירה "עטיפה" משלה.

| # | שכבה | תפקיד | דוגמאות | תקיפה אופיינית |
|---|------|-------|---------|-----------------|
| 7 | Application | ממשק לאפליקציה | HTTP, DNS, FTP, SSH | חולשות Web, פישינג |
| 6 | Presentation | הצפנה/קידוד | TLS/SSL | פענוח תעבורה |
| 5 | Session | ניהול חיבורים | RPC, NetBIOS | חטיפת Session |
| 4 | Transport | תעבורה מקצה-לקצה | TCP, UDP | סריקת פורטים |
| 3 | Network | ניתוב וכתובות לוגיות | IP, ICMP | Spoofing, ניתוב |
| 2 | Data Link | כתובות פיזיות | Ethernet, ARP, MAC | ARP Poisoning |
| 1 | Physical | אותות וכבלים | Wi-Fi, כבל | האזנה פיזית |

> 💡 **טריק לזכירה (מלמעלה למטה):** *All People Seem To Need Data Processing*.

**דוגמה להמחשה:** כשאתה שולח מייל — שכבה 7 יוצרת את ההודעה, שכבה 4 מחלקת אותה לחבילות TCP, שכבה 3 מוסיפה כתובות IP, שכבה 2 מוסיפה כתובות MAC, ושכבה 1 שולחת את הביטים. בצד השני התהליך הפוך.

---

## 3.3 מודל TCP/IP

**TCP/IP** הוא הגרסה המעשית (4 שכבות) שרשת האינטרנט משתמשת בה בפועל. הוא "מקפל" את שכבות OSI:

| TCP/IP | מקביל ל-OSI | דוגמאות |
|--------|-------------|---------|
| Application | `5–7` | HTTP, DNS, SSH |
| Transport | 4 | TCP, UDP |
| Internet | 3 | IP, ICMP |
| Network Access | `1–2` | Ethernet, ARP |

---

## 3.4 TCP מול UDP ולחיצת היד

בשכבת התעבורה שני פרוטוקולים עיקריים:

| | **TCP** | **UDP** |
|---|---|---|
| אמינות | אמין — מוודא הגעה | לא אמין — "יורה ושוכח" |
| חיבור | Connection-oriented | Connectionless |
| מנגנון | לחיצת יד תלת-שלבית | ללא |
| מהירות | איטי יותר | מהיר יותר |
| שימוש | web, מייל, קבצים | DNS, סטרימינג, VoIP |

### לחיצת היד התלת-שלבית (3-Way Handshake)
כך נפתח **כל** חיבור TCP — וזהו הבסיס לסריקת פורטים:
```text
לקוח  --- SYN --->      שרת      (בקשת חיבור)
לקוח  <-- SYN-ACK --    שרת      (אישור + בקשה חוזרת)
לקוח  --- ACK --->      שרת      (החיבור פתוח)
```

> 🔍 **קשר ישיר לתקיפה — כך nmap קובע אם פורט פתוח:**
> | תשובת השרת ל-SYN | מסקנה |
> |------------------|-------|
> | `SYN-ACK` | הפורט **פתוח** |
> | `RST` (Reset) | הפורט **סגור** |
> | אין תשובה | **מסונן** (Filtered — חומת אש) |
>
> סריקת "SYN Scan" (`nmap -sS`) שולחת SYN ולא משלימה את הלחיצה — מהירה ו"שקטה" יותר.

---

## 3.5 כתובות IP

**כתובת IP** מזהה מכשיר ברשת. שתי גרסאות:
- **IPv4:** ארבעה מספרים `(0–255)`, למשל `192.168.1.10`. 32 סיביות, בפורמט עשרוני (Decimal).
- **IPv6:** פורמט חדש, 128 סיביות, למשל `2001:0db8:85a3::8a2e:0370:7334`. בפורמט הקסדצימלי — נועד לפתור את מחסור הכתובות.

### פרטי מול ציבורי
- **פרטי (Private)** — לשימוש פנימי, אינו מנותב באינטרנט:
  - `10.0.0.0 – 10.255.255.255` (`/8`)
  - `172.16.0.0 – 172.31.255.255` (`/12`)
  - `192.168.0.0 – 192.168.255.255` (`/16`)
- **ציבורי (Public)** — ייחודי וגלוי באינטרנט.
- **Loopback** — `127.0.0.1` = המכונה עצמה (‏`localhost`‏).

> 💡 כשתראה `192.168.x.x` או `10.x.x.x` בבדיקה — אתה ברשת פנימית. זה מכתיב את הצעד הבא (Pivoting — מודול 12).

---

## 3.6 MAC ו-ARP

### MAC Address
כתובת פיזית קבועה של כרטיס הרשת, בשכבה 2.

- **פורמט:** `00:1A:2B:3C:4D:5E`
- "צרובה" בחומרה — אך **ניתנת לזיוף** (טכניקת `MAC Spoofing`).

### ARP — Address Resolution Protocol
פרוטוקול שמתרגם כתובת `IP` לכתובת `MAC` ברשת המקומית.

איך זה עובד: כשמכונה רוצה לשלוח מידע לכתובת מקומית, היא "צועקת" ברשת —
*"מי מחזיק את הכתובת הזו?"* —
והבעלים עונה בחזרה עם כתובת ה-`MAC` שלו.

### ARP Poisoning
מתקפת "איש בתווך" (**Man-in-the-Middle**) בשכבה 2.

התוקף עונה לבקשות `ARP` בשקר, וגורם לכל התעבורה לעבור דרכו —
וכך הוא מאזין לתקשורת. זו מתקפה קלאסית ברשת מקומית.

**פקודות לצפייה בטבלת ה-ARP:**
```bash
ip neigh      # מיפוי IP <-> MAC ידוע
arp -a        # תחביר ישן
```

---

## 3.7 פורטים ופרוטוקולים נפוצים

**פורט (Port)** הוא "דלת" ממוספרת `(0–65535)` שדרכה שירות מקבל תקשורת. זיהוי הפורטים הפתוחים = זיהוי השירותים = **משטח התקיפה**.

| פורט | פרוטוקול | שירות | הערה לתקיפה |
|---|---|---|---|
| 21 | TCP | FTP | העברת קבצים, לרוב לא מוצפן |
| 22 | TCP | SSH | גישה מרוחקת מאובטחת; יעד Brute Force |
| 23 | TCP | Telnet | גישה **לא** מוצפנת — מסוכן |
| 25 | TCP | SMTP | דואר יוצא |
| 53 | TCP/UDP | DNS | תרגום שמות |
| 80 | TCP | HTTP | web (לא מוצפן) |
| 110 | TCP | POP3 | קבלת דואר |
| 139/445 | TCP | SMB | שיתוף קבצים Windows — יעד מרכזי |
| 143 | TCP | IMAP | קבלת דואר |
| 443 | TCP | HTTPS | web מוצפן (TLS) |
| 3306 | TCP | MySQL | בסיס נתונים |
| 3389 | TCP | RDP | שולחן עבודה מרוחק Windows |

טווחי פורטים: **Well-Known** `(0–1023)`, **Registered** `(1024–49151)`, **Dynamic** `(49152–65535)`.

> 💡 שנן לפחות: 21, 22, 80, 443, 445, 3389 — הם חוזרים כמעט בכל בדיקה.

---

## 3.8 Subnetting (חלוקת רשת)

**Subnetting** הוא חלוקת רשת לתת-רשתות. עבור בודק חדירות, המטרה המעשית: **להבין את גבולות הרשת** — אילו כתובות שייכות לאותה תת-רשת (ולכן ייסרקו יחד).

### מסכת רשת ו-CIDR
- **מסכת רשת (Subnet Mask)** מפרידה בין חלק הרשת לחלק המארח (Host).
- **CIDR** מקצר זאת: המספר אחרי `/` = כמה סיביות שייכות לרשת.

| CIDR | מסכה | מס' כתובות | מארחים שמישים | דוגמה |
|------|------|-----------|----------------|-------|
| `/24` | 255.255.255.0 | 256 | 254 | `192.168.1.0/24` |
| `/16` | 255.255.0.0 | 65,536 | 65,534 | `172.16.0.0/16` |
| `/8` | 255.0.0.0 | ~16.7M | ~16.7M | `10.0.0.0/8` |

> 🔍 **דוגמה מעשית:** `nmap 192.168.1.0/24` = "סרוק את כל 256 הכתובות בתת-הרשת". `ipcalc 192.168.1.0/24` יראה לך את הכתובת הראשונה, האחרונה וה-Broadcast.

---

## 3.9 פרוטוקולי תשתית

### DNS — Domain Name System
מתרגם שם דומיין (למשל `example.com`) לכתובת `IP`.

- עובד בעיקר מעל `UDP`, פורט `53`.
- יעד מרכזי ל-OSINT ולתקיפות כמו `DNS Spoofing` ו-`Zone Transfer`.

### DHCP — Dynamic Host Configuration Protocol
מחלק כתובות `IP` אוטומטית למכשירים חדשים ברשת —
יחד עם כתובת ה-Gateway ושרת ה-DNS.

### NAT — Network Address Translation
הראוטר "מתרגם" כתובות פרטיות רבות לכתובת ציבורית אחת.
זו הסיבה שלכל המכשירים בבית יש כתובת `192.168.x.x`,
אך כלפי האינטרנט מופיעה כתובת ציבורית אחת בלבד.

### ICMP — Internet Control Message Protocol
פרוטוקול בקרה. הפקודה `ping` משתמשת בו כדי לבדוק אם מכונה "חיה".

> 🔍 **Zone Transfer** (הפקודה `AXFR`):
> אם שרת ה-DNS מוגדר שגוי, אפשר "לשאוב" ממנו את כל רשומות הדומיין — פרס ב-Recon:
> ```bash
> dig axfr @nsserver target.com
> ```

---

## 3.10 כלים מעשיים

```bash
ip a                       # כתובות IP ו-MAC
ip route                   # ה-Gateway
ping -c 4 8.8.8.8          # קישוריות (ICMP)
traceroute google.com     # מסלול החבילה (צמתים)
ss -tuln                   # פורטים מאזינים מקומית
nslookup google.com       # תרגום DNS
dig google.com            # שאילתת DNS מפורטת
ipcalc 192.168.1.0/24     # חישוב תת-רשת
```

### Wireshark ו-tcpdump — לראות את החבילות
- **Wireshark** — כלי גרפי ללכידת וניתוח תעבורה. מסננים שימושיים: `tcp.flags.syn==1`, `http`, `dns`.
- **tcpdump** — גרסת שורת הפקודה: `sudo tcpdump -i eth0 port 80`.

> 💡 **תרגיל מנטלי:** פתח Wireshark, גלוש לאתר, וזהה את רצף ה-SYN → SYN-ACK → ACK. לראות את הלחיצה "בחיים" מקבע את ההבנה.

---

## סיכום המודול

- **מסע חבילה**: DNS → ARP → TCP Handshake → HTTP; כל שלב הוא שכבה ונקודת תקיפה.
- **OSI (7)** ו-**TCP/IP (4)** מתארים את התקשורת; שכבה 4 = פורטים, שכבה 3 = IP, שכבה 2 = MAC.
- **TCP** אמין (Handshake) · **UDP** מהיר; תשובת SYN קובעת אם פורט פתוח/סגור/מסונן.
- **IP** (פרטי/ציבורי), **MAC/ARP**, **פורטים** ו-**Subnetting/CIDR** מגדירים את משטח התקיפה.
- **DNS/DHCP/NAT/ICMP** הם התשתית — וגם יעדים (Spoofing, Zone Transfer).

### מה הלאה?
➡️ [**תרגילים — `missions.md`**](missions.md) · [**פתרונות — `solutions.md`**](solutions.md) · [**תרגול ותרחישים — `practice.md`**](practice.md)
➡️ [**מודול 4 — סקריפטינג: Bash ו-Python**](../04-scripting-bash-python/)

[⬅️ חזרה למפת הקורס](../../README.md)

---

<a id="missions"></a>
# 🎯 תרגילים (Missions)

> ב-Terminal של Kali. פתרונות מלאים ב-[**`solutions.md`**](solutions.md).

> ### 🚦 לפני שמתחילים — קרא אותי!
> - **מה צריך:** מכונת **Kali** דולקת. לחלק ד' נשתמש ב-**Wireshark** (מותקן ב-Kali).
> - **🌐 חשוב — אינטרנט:** תרגילים **3.7** ו-**3.14** (ping/traceroute לאינטרנט) דורשים ש-Kali יהיה במצב רשת **NAT** (יש אינטרנט). אם הגדרת **Host-Only** (כמו שהומלץ למעבדת תקיפה) — **אין אינטרנט**, וה-ping ייכשל. פשוט החלף זמנית ל-NAT: `Settings → Network → Adapter 1 → NAT` (ולאחר מכן חזרה ל-Host-Only לתרגול תקיפה).
> - **איך עובדים:** קרא קודם את ה-[`README.md`](README.md). לשאלות ידע יש **💡 רמז** לסעיף המתאים; למשימות המעשיות (🟢) **הפקודה המדויקת + מה לחפש**.
> - **נתקעת?** קרא שוב את הסעיף ב-README (יש שם טבלאות עזר), ואז הצץ ב-[`solutions.md`](solutions.md). אם פקודה מחזירה `command not found` — התקן אותה עם `sudo apt install <שם> -y`.
> - **טיפ:** רשת היא הבסיס לכל סריקה — אם משהו לא ברור, אל תתקדם עד שהבנת.

מקרא רמות: 🟢 קל · 🟡 בינוני · 🟠 מתקדם · 🔴 אתגר

---

## חלק א' — ידע ומודלים (🟢 קל)

**3.1** — מנה את 7 שכבות מודל OSI מלמעלה למטה, ולכל שכבה תן פרוטוקול/דוגמה אחת.
  💡 רמז: README §3.2 (טבלת OSI). טריק לזכירה: *All People Seem To Need Data Processing*.
  ✅ הצלחה: 7 שכבות בסדר (Application→Physical) + דוגמה לכל אחת.

**3.2** — לאיזו שכבת OSI שייך כל אחד? HTTP · IP · Ethernet/MAC · TCP · ARP.
  💡 רמז: README §3.2.
  ✅ הצלחה: HTTP=7, TCP=4, IP=3, Ethernet/MAC=2, ARP=2.

**3.3** — הסבר את ההבדל בין TCP ל-UDP, ותן שירות אחד לכל אחד.
  💡 רמז: README §3.4.
  ✅ הצלחה: TCP=אמין/מבוסס-חיבור (HTTP), UDP=מהיר/ללא חיבור (DNS).

**3.4** — כתוב את שלושת שלבי ה-3-Way Handshake לפי הסדר.
  💡 רמז: README §3.4.
  ✅ הצלחה: SYN → SYN-ACK → ACK.

---

## חלק ב' — הכתובות שלי (🟢🟡)

**3.5** 🟢 — מצא את כתובת ה-IP, ה-MAC וה-Gateway של Kali.
```bash
ip a          # IP (inet) ו-MAC (link/ether) תחת eth0
ip route      # Gateway (default via ...)
```
  👀 **חפש:** `inet` = ה-IP; `link/ether` = ה-MAC; `default via X` = ה-Gateway.
  ✅ הצלחה: רשמת את שלושת הערכים.

**3.6** 🟢 — האם ה-IP שלך פרטי או ציבורי? לפי איזה טווח קבעת?
  💡 רמז: README §3.5 (טווחים פרטיים: 10.x, 172.16–31.x, 192.168.x).
  ✅ הצלחה: קבעת נכון + ציינת את הטווח.

**3.7** 🟡 — הרץ `ping` ל-IP ואז לשם, וזהה מי תרגם את השם. *(דורש אינטרנט — מצב NAT; ראה ההערה למעלה.)*
```bash
ping -c 4 8.8.8.8
ping -c 4 google.com
```
  👀 **חפש:** לפני התשובה מ-google.com מופיע ה-IP שלו — **DNS** תרגם את השם ל-IP.
  ✅ הצלחה: הבנת ש-DNS ביצע את התרגום משם לכתובת IP. *(אם מקבל `Destination Host Unreachable` — אתה כנראה ב-Host-Only; עבור ל-NAT.)*

**3.8** 🟡 — הצג את טבלת ה-ARP של המכונה.
```bash
ip neigh      # או: arp -a
```
  👀 **חפש:** זוגות של IP ↔ MAC של מכונות ברשת המקומית.
  ✅ הצלחה: ראית את מיפוי ה-IP↔MAC.

---

## חלק ג' — פורטים ותת-רשתות (🟡🟠)

**3.9** 🟡 — מלא מהזיכרון: הפרוטוקול/שירות עבור 21, 22, 80, 443, 445, 3389.
  💡 רמז: README §3.7 (טבלת הפורטים).
  ✅ הצלחה: 21=FTP, 22=SSH, 80=HTTP, 443=HTTPS, 445=SMB, 3389=RDP.

**3.10** 🟠 — עבור `10.10.50.0/24`: כמה כתובות? מסכה? כמה מארחים שמישים?
```bash
sudo apt install ipcalc -y     # אם עדיין לא מותקן (לרוב לא מגיע מראש)
ipcalc 10.10.50.0/24
```
  👀 **חפש:** `Netmask` (255.255.255.0), `Hosts/Net` (254).
  ✅ הצלחה: 256 כתובות, מסכה /24, 254 מארחים שמישים.

**3.11** 🟠 — אם תריץ `nmap 172.16.0.0/16`, כמה כתובות תיאורטית ייסרקו? האם זה רעיון טוב?
  💡 רמז: /16 = 2 בתים למארחים = 256×256.
  ✅ הצלחה: ~65,536 כתובות; רעיון גרוע לסריקה מלאה (איטי ורועש) — עדיף לצמצם טווח.

---

## חלק ד' — לראות את הרשת בעיניים (🟠 מתקדם)

**3.12** — לכוד את ה-Handshake ב-Wireshark.
```bash
# פתח Wireshark → התחל לכידה על eth0, ואז בטרמינל:
curl http://example.com
```
  👀 **חפש:** מסנן `tcp` — אתר את רצף **SYN → SYN-ACK → ACK**. צרף צילום מסך.
  ✅ הצלחה: זיהית את שלוש חבילות פתיחת החיבור.

**3.13** — ב-Wireshark, סנן `dns`, גלוש לאתר, וזהה Query ו-Response.
  💡 רמז: השאילתה (Query) שואלת "מה ה-IP?" והתשובה (Response) מחזירה אותו.
  ✅ הצלחה: ראית זוג Query/Response של תרגום שם.

**3.14** — הרץ `traceroute` וספור צמתים.
```bash
traceroute google.com
```
  👀 **חפש:** כל שורה = צומת (Hop) בדרך ליעד; ספור כמה יש.
  ✅ הצלחה: יש לך מספר Hops והבנת שכל שורה = ראוטר בדרך.

---

## 🔴 אתגר מסכם — "מפת הרשת שלי"

1. **מפה:** שרטט את מפת המעבדה — Kali, ה-Gateway, שרת ה-DNS — עם כתובות IP ו-MAC.
2. **Handshake חי:** צרף צילום מסך מ-Wireshark של שלוש חבילות פתיחת החיבור.
3. **ניתוח פורטים:** הסבר במשפט לכל פורט — מדוע 445 (SMB) ו-3389 (RDP) הם יעדי תקיפה מרכזיים.
4. **תת-רשת:** בהינתן שאתה ב-`192.168.1.50/24`, מה טווח הכתובות שתסרוק במודול 6, ובאיזו פקודת nmap?
5. **מסקנה:** 2–3 שורות — מהרשת שמיפית, מה הצעד הבא ההגיוני.

  💡 רמז: כל הנתונים כבר בתרגילים 3.5–3.14. טווח לסריקה: `192.168.1.0/24` עם `nmap -sn`.
  ✅ הצלחה: מסמך עם מפה, צילום Handshake, וניתוח + מסקנה.

> 📤 שמור — זו התשתית שעליה נבצע סריקה אקטיבית במודול 6.

---

## ✅ רשימת בקרה — לפני מעבר למודול 4
- [ ] אני מכיר את שכבות OSI ומה שייך לכל אחת
- [ ] אני מבין TCP מול UDP ואת ה-3-Way Handshake
- [ ] אני יודע למצוא IP/MAC/Gateway ולזהות רשת פרטית
- [ ] אני זוכר את הפורטים הנפוצים
- [ ] אני מבין CIDR (`/24`) וגבולות תת-רשת
- [ ] ראיתי לחיצת יד ותרגום DNS ב-Wireshark

*פתרונות מלאים: [`solutions.md`](solutions.md)*

---

<a id="practice"></a>
# 🧠 תרגול (Practice)

> חלק א' — **שאלות תרגול** (עם תשובות). חלק ב' — **תרחיש מהחיים האמיתיים**.

---

## חלק א' — שאלות תרגול

**Q1.** מנה את 7 שכבות OSI, ולכל אחת פרוטוקול לדוגמה.

**Q2.** מהם שלושת שלבי ה-3-Way Handshake?

**Q3.** בסריקת nmap: איזו תשובה מעידה שפורט פתוח? סגור? מסונן?

**Q4.** לאיזה שירות שייך כל פורט: 21, 22, 80, 443, 445, 3389?

**Q5.** מה ההבדל בין TCP ל-UDP? תן שירות לכל אחד.

**Q6.** כמה כתובות ב-`/24`? כמה שמישות? ומה הפקודה לחישוב?

**Q7.** מה עושה ARP, ומהי מתקפת ARP Poisoning?

**Q8.** הכתובת `172.20.5.10` — פרטית או ציבורית? לפי איזה טווח?

### ✅ תשובות
1. Application(HTTP), Presentation(TLS), Session(RPC), Transport(TCP), Network(IP), Data Link(Ethernet/ARP), Physical(כבל).
2. SYN → SYN-ACK → ACK.
3. פתוח=SYN-ACK; סגור=RST; מסונן=אין תשובה.
4. 21=FTP, 22=SSH, 80=HTTP, 443=HTTPS, 445=SMB, 3389=RDP.
5. TCP אמין ומבוסס-חיבור (HTTP/מייל); UDP מהיר וללא חיבור (DNS/סטרימינג).
6. 256 כתובות, 254 שמישות; `ipcalc 192.168.1.0/24`.
7. ARP מתרגם IP↔MAC ברשת מקומית; Poisoning = תוקף עונה בשקר ומיירט תעבורה (MITM).
8. פרטית — טווח 172.16.0.0/12.

---

## חלק ב' — תרחיש מהחיים האמיתיים

### 🔍 "ניתוח לחיצת יד ב-Wireshark"
אתה בבדיקה פנימית ורוצה להבין את התעבורה ברשת. **בצע במעבדה:**

```bash
# טרמינל 1 — פתח Wireshark (או tcpdump):
sudo wireshark &
# בסינון הקלד: tcp.flags.syn==1
# טרמינל 2 — צור תעבורה:
curl http://example.com
```

**המשימה:**
1. אתר ב-Wireshark את שלוש חבילות פתיחת החיבור. איזו יצאה ממך, ואיזו מהשרת?
2. שנה את הסינון ל-`dns`. גלוש לאתר וזהה את שאילתת התרגום. איזה פרוטוקול תעבורה (TCP/UDP) ואיזה פורט?
3. אם היית תוקף ברשת זו — אילו שתי מתקפות שכבה-2/3 היו רלוונטיות למה שראית?

### 💡 פתרון מנחה
1. **SYN** יצא ממך (הלקוח), **SYN-ACK** מהשרת, ואז **ACK** ממך — פתיחת החיבור.
2. DNS פועל מעל **UDP פורט 53**; תראה `Standard query` ו-`query response`.
3. **ARP Poisoning** (MITM על שכבה 2) לאיסוף תעבורה, ו-**DNS Spoofing** (זיוף תשובת DNS) להפניית הקורבן לשרת זדוני.

> 🎯 לראות את המנגנונים "בחיים" (Handshake, DNS) הופך את התיאוריה למוחשית — וזה בסיס להבנת סריקות (מודול 6) ומתקפות רשת.

[⬅️ חזרה למודול](README.md) · [תרגילים מעשיים](missions.md)

---

<a id="solutions"></a>
# ✅ פתרונות (Solutions)

> נסה לבד ב-[`missions.md`](missions.md) קודם.

---

## חלק א' — ידע ומודלים

**3.1 — 7 שכבות OSI (מלמעלה למטה):**
7. Application (HTTP) · 6. Presentation (TLS) · 5. Session (RPC) · 4. Transport (TCP/UDP) · 3. Network (IP) · 2. Data Link (Ethernet/ARP) · 1. Physical (כבל/Wi-Fi).

**3.2 — שיוך לשכבות:**
- HTTP → שכבה 7 (Application)
- IP → שכבה 3 (Network)
- Ethernet/MAC → שכבה 2 (Data Link)
- TCP → שכבה 4 (Transport)
- ARP → שכבה 2 (Data Link)

**3.3 — TCP מול UDP:**
TCP אמין ומבוסס-חיבור (לחיצת יד, מוודא הגעה) — למשל HTTP/מייל. UDP מהיר וללא חיבור ("יורה ושוכח") — למשל DNS/סטרימינג.

**3.4 — 3-Way Handshake:**
1. Client → SYN → Server
2. Client ← SYN-ACK ← Server
3. Client → ACK → Server (החיבור פתוח)

---

## חלק ב' — הכתובות שלי

**3.5**
```bash
ip a        # שורת inet = IP, שורת link/ether = MAC
ip route    # default via X.X.X.X = ה-Gateway
```

**3.6** — אם קיבלת `192.168.x.x` → פרטי (טווח 192.168.0.0/16). `10.x.x.x` → פרטי (10.0.0.0/8). כתובת אחרת שאינה בטווחים הפרטיים → ציבורית.

**3.7**
```bash
ping -c 4 8.8.8.8       # IP ישיר — אין צורך ב-DNS
ping -c 4 google.com    # קודם DNS מתרגם את השם ל-IP, ואז ping
```
ההבדל: בשם צריך תחילה תרגום **DNS**; רואים בפלט את ה-IP שהתקבל.

**3.8**
```bash
ip neigh     # (או arp -a)
```
הטבלה מכילה מיפוי בין כתובות IP מקומיות לכתובות ה-MAC המתאימות — מכשירים שהמכונה תקשרה איתם לאחרונה.

---

## חלק ג' — פורטים ותת-רשתות

**3.9** — 21/TCP FTP · 22/TCP SSH · 80/TCP HTTP · 443/TCP HTTPS · 445/TCP SMB · 3389/TCP RDP.

**3.10** — `10.10.50.0/24`:
- סך כתובות: **256**
- מסכה: **255.255.255.0**
- מארחים שמישים: **254** (פחות כתובת הרשת וה-Broadcast)
```bash
sudo apt install ipcalc -y     # אם צריך
ipcalc 10.10.50.0/24
```

**3.11** — `/16` = **65,536** כתובות. לסרוק את כולן זה **לא** רעיון טוב בבת אחת — זה איטי מאוד ורועש (מתגלה בקלות). עדיף קודם **Host Discovery** (`nmap -sn 172.16.0.0/16`) כדי למצוא מכונות חיות, ואז לסרוק רק אותן.

---

## חלק ד' — לראות את הרשת בעיניים

**3.12** — ב-Wireshark, מסנן `tcp.flags.syn==1` יעזור לאתר. תראה:
1. חבילה עם דגל SYN (ממך לשרת)
2. חבילה עם SYN+ACK (מהשרת)
3. חבילה עם ACK (ממך)

**3.13** — מסנן `dns` יראה שתי חבילות: **Standard query** (השאלה "מה ה-IP של X?") ו-**Standard query response** (התשובה עם ה-IP). זהו שכבה 7 מעל UDP פורט 53.

**3.14**
```bash
traceroute google.com
```
כל שורה = צומת (Router) אחד בדרך ליעד, עם זמן התגובה שלו. מספר ה-Hops הוא מספר השורות. הצמתים הראשונים הם ברשת שלך/הספק.

---

## 🔴 אתגר מסכם — קווים מנחים

1. **מפה:** דיאגרמה עם Kali (למשל 192.168.1.50), Gateway (192.168.1.1), DNS.
2. **Handshake:** צילום Wireshark של SYN/SYN-ACK/ACK.
3. **פורטים:** 445 (SMB) — שיתוף קבצים Windows, וקטור לתנועה רוחבית ולחולשות כמו EternalBlue. 3389 (RDP) — גישה גרפית מרוחקת, יעד ל-Brute Force ולגישה ראשונית.
4. **תת-רשת:** ב-`/24` תסרוק את `192.168.1.0/24` (256 כתובות) עם `nmap -sn 192.168.1.0/24` ואז סריקת שירותים על החיות.
5. **מסקנה לדוגמה:** "מיפיתי רשת /24 עם Gateway ב-.1. הצעד הבא: Host Discovery כדי למצוא מכונות חיות, ואז סריקת פורטים ושירותים (מודול 6) על היעדים שיימצאו."

> עברת? המשך ל[מודול 4](../04-scripting-bash-python/).

</div>
